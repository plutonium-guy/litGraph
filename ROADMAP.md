# litGraph Roadmap

Research-backed prioritization of remaining gaps vs. LangChain + LangGraph,
with a focus on items where Rust gives a real (not cosmetic) advantage:
true parallelism, zero-cost abstractions, no GIL, no Python class-soup.

This is a *living* doc — picked up at the start of each iteration to
choose the next ship target. Numbered items below get crossed off as
they land in `git log` (look for `iter N`).

Snapshot date: 2026-04-29 · last shipped: iter 180 (Studio debug
router for `litgraph-serve`, feature-gated). Tracker of *what's done*
lives in `FEATURES.md` and `CLAUDE_CODE_FEATURES.md`; this doc is
*what's next*.

---

## Guiding philosophy (recap)

1. **Rust does the work, Python is the shim.** Hot paths (HTTP, SSE
   parse, tokenize, embed math, vector search, JSON parse, graph
   scheduling, RRF, MMR) are Rust. Python is `#[pyfunction]` and
   `extract_chat_model`.
2. **Parallelism is a first-class feature.** Tokio for I/O fan-out;
   Rayon for CPU-bound batching; `py.allow_threads` (PyO3 0.22+ →
   `py.detach`) around every blocking call so the GIL is dropped.
3. **No bloat.** Each capability is its own crate behind a feature
   flag. Default features stay tight; users opt in.
4. **Graph-first, no Runnable cathedral.** A `StateGraph` + a few
   functions cover what LangChain spreads across `LCEL`, `Runnable`,
   `Chain`, `Memory`, and N variants of each.

If a gap doesn't pay rent against (1)–(4), it goes under the
"won't do" list at the bottom.

---

## Prioritization rubric

Each candidate scores on five axes (1–5, higher = more compelling):

| Axis              | What it asks                                           |
|-------------------|--------------------------------------------------------|
| **User impact**   | How often does a real agent author hit this?           |
| **Parity weight** | Does its absence stop people picking litGraph?         |
| **Rust win**      | Does Rust make this materially faster/safer than LC?   |
| **Surface area**  | How many crates does it touch? (lower → easier ship)   |
| **Test cost**     | Can it be unit-tested without a paid LLM?              |

Total = sum. Anything ≥18 is fair game for the next ten iters.

---

## Tier 1 — ship in the next ~10 iters

### 1. EnsembleRetriever — weighted RRF ✅ shipped iter 181
- **Status:** ✅. `litgraph_retrieval::EnsembleRetriever` — per-child
  weights, weighted RRF, `tokio::join_all` fan-out. Python:
  `litgraph.retrieval.EnsembleRetriever`.
- **What:** `Vec<(Arc<dyn Retriever>, f32)>` fanned out via
  `tokio::join_all`, fused with weighted reciprocal rank
  fusion: `score(d) = Σ_i w_i / (k_rrf + rank_i(d))`.
- **Why:** LangChain's `EnsembleRetriever` is the canonical recipe
  for combining a sparse (BM25) + dense (vector) retriever where
  the sparse one is noisier and should get less weight. Equal-weight
  RRF is suboptimal; production users tune ratios.
- **Rust win:** all child retrievers run truly concurrent. Python's
  `EnsembleRetriever` runs sequentially under the GIL.
- **Effort:** 1 iter. New file `ensemble.rs`, ~150 LOC + tests.

### 2. `before_tool` / `after_tool` middleware hooks
- **Status:** 🟡. `before/after_model` exists. Tool wrappers
  (`RetryTool`, `TimeoutTool`, `OffloadingTool`) cover
  point use-cases, but a generic chain over the agent loop's tool
  invocations is still missing.
- **What:** Extend `MiddlewareChain` to intercept the tool-execution
  branch of `ReactAgent` / `SupervisorAgent`. Same trait shape as
  the model hooks.
- **Why:** First-class observability + cost control across all
  tools without per-tool wrapping.
- **Rust win:** zero per-call cost when chain is empty (compiles
  out via monomorphization).
- **Effort:** 1–2 iters. Touches agents + middleware crates.

### 3. Vector-indexed semantic search on `Store` ✅ shipped iter 185
- **Status:** ✅ in-memory tier. `litgraph_core::SemanticStore` wraps
  any `Store` with an `Embeddings` provider; `semantic_search` runs
  brute-force Rayon-parallel cosine over a namespace.  Python:
  `litgraph.store.SemanticStore(store, embedder)`.
- **Remaining:** pgvector-backed indexed search on `PostgresStore`
  for the >10k items/namespace tier.

### 4. Postgres `Store` already shipped → wire vector index
- **Status:** ✅ structurally, 🟡 functionally. `PostgresStore`
  exists. Adding a `pgvector` column for the `value` JSON's optional
  embedding completes #3 for distributed deployments.

### 5. Functional API: `@entrypoint` + `@task` (Python)
- **Status:** ❌.
- **What:** Decorator-based alternative to `StateGraph` for simple
  workflows. `@entrypoint` marks the root, `@task` marks a
  sub-routine; the runtime auto-builds the graph.
- **Why:** Lets a 5-line script author skip `add_node` / `add_edge`
  ceremony.
- **Rust win:** thin — Rust core already runs the graph. This is a
  Python-side ergonomics layer.
- **Effort:** 1 iter (py-only).

### 6. `pyo3-stub-gen` auto-generated `.pyi`
- **Status:** 🟡. Hand-written stubs in `litgraph-stubs/` drift.
- **What:** Wire `pyo3-stub-gen` so stubs regenerate on every
  `maturin build`.
- **Why:** Pyright import warnings hurt agent-authored code (and
  Claude Code's own UX when editing user repos that import
  `litgraph`).
- **Effort:** 1 iter.

### 7. Pydantic-coerced state + StreamPart on Python side
- **Status:** ❌.
- **What:** `litgraph.StateModel` (Pydantic v2 base) so the Python
  state dict gets validated/typed; `StreamPart` discriminated
  union for stream events.
- **Why:** IDE autocomplete on stream chunks. Today everything is
  `dict[str, Any]`.
- **Effort:** 1–2 iters.

### 8. Local chat model — candle / mistral.rs
- **Status:** ❌. Embeddings have a local path (fastembed), chat
  doesn't. This is the biggest "air-gapped agent" gap.
- **What:** New crate `litgraph-providers-local` wrapping
  `mistralrs` (or `candle` directly) behind the `ChatModel` trait.
- **Why:** Full offline agent. Strong differentiator from LangChain
  whose local stories all go through `ollama` or `llama.cpp`
  external processes.
- **Rust win:** in-process inference, no IPC; can share GPU
  context with embeddings.
- **Effort:** 3–4 iters. Heavy because of model loading + KV-cache
  + tokenization edge cases.

### 9. Webhook-resume bridge for interrupts ✅ shipped iter 201 + 202
- **Status:** ✅. `litgraph_core::ResumeRegistry` (201) ships the
  oneshot-backed coordination primitive; `litgraph_serve::resume::
  resume_router` (202) wraps it in axum endpoints — `POST
  /threads/:id/resume`, `DELETE /threads/:id/resume`,
  `GET /resumes/pending`. Mounts alongside the chat / studio routers.

### 10. Pregel-style super-step parallel execution audit
- **Status:** ✅ scheduler exists, but worth a perf pass — currently
  Send fan-out runs futures concurrently but always on the same
  Tokio runtime; nodes that are CPU-bound (e.g. running a local
  embedder inline) block the runtime. Audit + add a
  `spawn_blocking` escape hatch for CPU-heavy nodes.
- **Effort:** 1 iter.

---

## Tier 2 — useful but lower priority

- WhatsApp / Telegram **history** loaders (Telegram is push-only;
  no usable history. WhatsApp Business API is feasible but
  paperwork-heavy.)
- WebSocket streaming endpoint on `litgraph-serve` (today: SSE
  covers 95% of use-cases; WS is a nice-to-have for back-channel
  cancellation).
- Video-in modality — needs provider-side support, not framework
  work.
- Sentence/NLTK/SpaCy splitters — recursive char + token splitter
  cover the cases. Skip unless someone files a real ask.
- LanceDB / Pinecone vector stores — heavy deps, low marginal
  value over hnsw + pgvector + qdrant + chroma + weaviate.

## Won't do (deferred indefinitely)

- LangChain `Callbacks` API parity — surface area is enormous and
  `CostTracker + GraphEvent + AgentEvent + tracing` already covers
  every concrete use-case asked for.
- Streaming tool execution — would require a `Tool` trait revamp
  (yielding tokens from a tool). The few real use-cases (long
  shell jobs) are better served by `OffloadingTool` + a follow-up
  read.
- Zapier / N8N tool — userland integration; out of framework scope.

---

## Parallelism showcases — where Rust earns its keep

These are existing primitives that already use real parallelism;
listed here as a reference target ("when adding X, follow these
patterns").

| Primitive                   | Pattern                       | Why this is a Rust win |
|-----------------------------|-------------------------------|------------------------|
| `HybridRetriever`           | `tokio::join_all` over kids   | LangChain runs them sequential under GIL |
| `MultiQueryRetriever`       | `JoinSet` over N variants     | Same — N parallel embed+search |
| `parallel_ingest` (Rayon)   | CPU fan-out on splitters      | Pure CPU, GIL would kill it |
| Eval harness                | bounded `Semaphore` fan-out   | Lets a 1k-case suite run with 50-way concurrency safely |
| Send fan-out scheduler      | per-node `tokio::spawn`       | Pregel-style supersteps |
| `RerankingRetriever` batch  | one model call for N docs     | Tower of 1 vs N |
| `EnsembleRetriever` (iter 181) | weighted RRF + `join_all`  | Per-child weights with parallel fan-out — LangChain serialises |
| `batch_concurrent` (iter 182) | `Semaphore` + `JoinSet`     | Bounded-concurrency batch over any `ChatModel`; order-preserving, per-call `Result` |
| `embed_documents_concurrent` (iter 183) | chunk + `JoinSet` | Splits inputs into chunks, fans chunks across `Semaphore`; aligned output, fail-fast |
| `RaceChatModel` (iter 184) | `JoinSet` + `abort_all` | Hedged-request pattern: invoke N concurrently, first success wins, losers cancelled. Tail-latency reduction across providers/regions |
| `SemanticStore` (iter 185) | Rayon `par_iter` cosine + `par_sort` | Brute-force semantic search on any `Store`; Rayon makes it CPU-saturating across cores. Python's `numpy` would still hit the GIL on the gather step |
| `EnsembleReranker` (iter 186) | `tokio::join_all` over rerankers | Fans N rerankers over the SAME candidates concurrently; weighted RRF on ranks (scale-free across providers). LangChain's reranker chains run sequential |
| `load_concurrent` (iter 187) | `spawn_blocking` + `Semaphore` | Sync `Loader::load()` calls fan out onto Tokio's blocking pool; aligned output, per-loader `Result`. LangChain's loader chains are sequential by convention |
| `MultiVectorRetriever` (iter 188) | composes `embed_documents_concurrent` | Indexing N perspectives per parent uses chunked Tokio fan-out from iter 183. Retrieval dedups by parent_id in linear time |
| `multiplex_chat_streams` (iter 189) | `tokio::mpsc` channel-fan-in | Per-stream task forwards events into a shared channel; outer Stream drains in arrival order. First channel-based primitive — distinct from JoinSet/Semaphore lineage; one slow/failing stream never stalls peers |
| `retrieve_concurrent` (iter 190) | Tokio `JoinSet` + `Semaphore` over `Retriever::retrieve` | Completes the parallel-batch trio (`batch_concurrent`/`embed_documents_concurrent`/this). Same retriever, N caller queries — eval / agentic batch path |
| `tool_dispatch_concurrent` (iter 191) | Tokio `JoinSet` + `Semaphore` over heterogeneous `Tool::run` | Fourth in the parallel-batch family — different topology (HashMap router, heterogeneous tools per call). Standalone helper for Plan-and-Execute and custom orchestrators outside the React loop |
| `RaceEmbeddings` (iter 192) | Tokio `JoinSet` + `abort_all` over `Embeddings` | Embeddings analogue of iter 184 `RaceChatModel`. Hedge OpenAI / Voyage / local fastembed for tail-latency cuts on the embed-query critical path |
| `RaceRetriever` (iter 193) | Tokio `JoinSet` + `abort_all` over `Retriever` | Completes the race trio across the read-side traits (Chat/Embeddings/Retriever). Hedge a fast cache against a slow primary for latency-min retrieval; vs `EnsembleRetriever` which fuses for quality |
| `TimeoutChatModel` + `TimeoutEmbeddings` (iter 194) | `tokio::time::timeout` (concurrent inner-future vs deadline-timer, `select`-style) | Different shape from `JoinSet/abort_all` race patterns: the "competitor" is a deadline timer, not another provider. Drops the inner future on timeout, releasing connection / parse state |
| `broadcast_chat_stream` (iter 195) | `tokio::sync::broadcast` (1→N fan-out) | Inverse of iter 189's `mpsc`-based fan-in. One upstream stream, N concurrent subscribers; lazy-spawn pump to avoid event loss vs late subscribers; `Lagged(n)` per subscriber on capacity overflow so a slow consumer doesn't stall fast ones |
| `ingest_to_stream` (iter 196) | Multi-stage `mpsc` pipeline | First **pipeline-parallelism** primitive: three Tokio tasks (load / split / embed) connected by bounded `mpsc` channels, each stage runs while later stages drain. Composes iter 187 (load_concurrent) + a splitter closure + iter 183 (embed_documents_concurrent) with backpressure between stages |
| `rerank_concurrent` (iter 197) | Tokio `JoinSet` + `Semaphore` over `Reranker::rerank` | Adds a fifth axis to the parallel-batch family (chat/embed/retrieve/tool/rerank). One reranker, N independent `(query, candidates)` pairs — eval / batch-rerank path |
| `Bm25Index::add` parallel build (iter 198) | Rayon `par_iter` on tokenize+count per doc | Pure CPU parallelism for index construction (vs the I/O-bound parallelism of the JoinSet/Semaphore family). Each doc tokenizes independently in a Rayon worker; DF merge happens sequentially under the write lock |
| `Progress<T>` (iter 199) | `tokio::sync::watch` latest-value broadcast | Completes the channel-shape trio: mpsc fan-in (189), broadcast fan-out (195), watch latest-value (199). Multiple observers, intermediate values collapse — observers see only the latest snapshot, perfect for progress UIs |
| `ingest_to_stream_with_progress` (iter 200) | Composition of iter 196 + iter 199 | First **composition** of two prior parallelism primitives: the multi-stage ingest pipeline (196) updates an `IngestProgress` watcher (199) at each stage boundary. Demonstrates the compositional payoff of building primitives that snap together |
| `ResumeRegistry` (iter 201) | `tokio::sync::oneshot` per thread id | Fourth channel shape in the lineage: oneshot signal (single-fire, single consumer). Foundation for the LangGraph interrupt-resume pattern — agent parks on `await_resume()`, an HTTP handler / Slack callback fires `resume(thread_id, value)` from anywhere |
| `resume_router` (iter 202) | axum router over `ResumeRegistry` | Wire-protocol completion of iter 201: `POST /threads/:id/resume {value}` delivers, `DELETE /threads/:id/resume` cancels, `GET /resumes/pending` lists. Composes a coordination primitive into a real prod-ready HTTP feature in <100 LOC of glue |
| `mmr_select` parallel (iter 203) | Rayon `par_iter` on per-candidate scoring | The greedy-pick outer loop stays sequential (each pick depends on prior picks); the per-iteration score loop is now Rayon-parallel — independent O(\|picked\|) cosine sims per candidate. Deterministic tie-break on lower index keeps parallel picks bit-identical to a sequential reference |
| `embedding_redundant_filter` parallel (iter 204) | Rayon `par_iter::any` with short-circuit | Sibling pattern to iter 203: outer kept-list loop sequential, inner "is candidate similar to ANY kept doc?" probe parallelizes across cores via `any` (which short-circuits on first hit, just like the sequential `for ... break`). 3 cross-impl tests cover small / larger / threshold-extreme pools |
| `batch_concurrent_with_progress` (iter 205) | Composes iter 182 + iter 199 | Second progress-aware composition (after iter 200's progress-aware ingestion). `BatchProgress { total, completed, errors }` watcher updated as each `ChatModel.invoke` completes — drop-in replacement for `batch_concurrent` when the caller wants live eval-harness counters |
| `embed_documents_concurrent_with_progress` (iter 206) | Composes iter 183 + iter 199 | Third progress-aware composition. `EmbedProgress { total_texts, total_chunks, completed_chunks, completed_texts, errors }` watcher updated as each chunk's embed call returns — drop-in replacement for bulk-indexing flows that want a tqdm-style counter / ETA |
| `retrieve_concurrent_with_progress` (iter 207) | Composes iter 190 + iter 199 | Fourth progress-aware composition. `RetrieveProgress { total, completed, docs_returned, errors }` watcher updated as each query completes — eval-harness pattern with a live counter |
| `tool_dispatch_concurrent_with_progress` (iter 208) | Composes iter 191 + iter 199 | Fifth progress-aware composition. `ToolDispatchProgress { total, completed, errors, unknown_tool_errors }` — unknown-tool errors bucketed separately so dashboards distinguish a routing-bug regression (LLM emitted a name your registry doesn't know) from a runtime tool failure |
| `rerank_concurrent_with_progress` (iter 209) | Composes iter 197 + iter 199 | Closes the progress-aware family across all 6 parallel-batch axes (ingest, chat batch, embed batch, retriever, tool, rerank). `RerankProgress { total, total_candidates, completed, docs_returned, errors }` — `total_candidates` exposed separately so eval reports can compute "X% of candidates reranked" rather than just "X% of pairs" |
| `batch_concurrent_stream` (iter 210) | mpsc-backed streaming variant of iter 182 | Yields `(idx, Result<ChatResponse>)` pairs in completion order as each invoke finishes, instead of buffering the whole `Vec`. Caller can render results live, dispatch downstream work on early completers, or drop the stream to abort remaining in-flight work (the spawned pump calls `set.abort_all()` on receiver-drop). First "stream-out" variant in the parallel-batch family — same primitive, different consumer shape |
| `embed_documents_concurrent_stream` (iter 211) | mpsc-backed streaming variant of iter 183 | Streaming-variant pattern extended to the embeddings axis. Yields `(chunk_idx, Result<Vec<Vec<f32>>>)` as each chunk's embed call returns; bulk indexers upsert chunks into a vector store on early completers without waiting for the slowest. Same abort-on-drop semantics as iter 210 |
| `retrieve_concurrent_stream` (iter 212) | mpsc-backed streaming variant of iter 190 | Streaming-variant pattern extended to the retriever axis. Yields `(query_idx, Result<Vec<Document>>)` as each query completes — eval harnesses can render rows live; drop the stream to abort the rest |
| `tool_dispatch_concurrent_stream` (iter 213) | mpsc-backed streaming variant of iter 191 | Streaming-variant pattern extended to the tool dispatch axis. Yields `(call_idx, Result<Value>)` as each tool call completes — orchestrators can chain follow-ups on fast tools without waiting for the slowest |
| `rerank_concurrent_stream` (iter 214) | mpsc-backed streaming variant of iter 197 | Streaming-variant pattern extended to the rerank axis. Yields `(pair_idx, Result<Vec<Document>>)` as each rerank call completes; closes 5 of 6 streaming-variant axes |
| `load_concurrent_stream` (iter 215) | mpsc-backed streaming variant of iter 187 | Streaming-variant pattern extended to the loader axis. Yields `(loader_idx, LoaderResult<Vec<Document>>)` as each blocking `load()` returns from `spawn_blocking`. Sixth distinct primitive in the streaming family — alongside the buffered-Vec and progress-aware patterns, the parallel-batch toolbox now offers three consumer shapes per axis |
| `batch_concurrent_stream_with_progress` (iter 216) | Composes iter 205 + iter 210 | First combination of two consumer shapes (stream + watcher) in one call. Per-row events drive a UI list view while `BatchProgress { total, completed, errors }` drives a summary progress bar — both backed by the same batch task. Demonstrates the four-quadrant matrix per axis: buffered Vec / progress-aware Vec / streaming / streaming-with-progress |
| `embed_documents_concurrent_stream_with_progress` (iter 217) | Composes iter 206 + iter 211 | Combined consumer shape extended to the embed axis. Both chat-batch and embed-batch axes now ship all four quadrants. Same consistency contract as iter 216 (counter ticks before stream item) so observers and stream consumers see synchronized state |
| `retrieve_concurrent_stream_with_progress` (iter 218) | Composes iter 207 + iter 212 | Combined consumer shape extended to the retriever axis. Three of six axes now ship the full four-quadrant matrix |
| `tool_dispatch_concurrent_stream_with_progress` (iter 219) | Composes iter 208 + iter 213 | Combined consumer shape extended to the tool axis. Four of six axes now ship the full four-quadrant matrix. `unknown_tool_errors` bucketing carries over so observers see routing-vs-runtime breakdown live |
| `rerank_concurrent_stream_with_progress` (iter 220) | Composes iter 209 + iter 214 | Combined consumer shape extended to the rerank axis. Five of six axes now ship the full four-quadrant matrix |
| `load_concurrent_with_progress` + `load_concurrent_stream_with_progress` (iter 221) | Closes the loader axis | Two functions in one iter — the loader axis was missing the progress-aware variant since iter 187, so this iter retroactively ships it AND the combined stream-with-progress sibling. The four-quadrant consumer matrix now closes across all six parallel-batch axes |
| `SemanticStore::bulk_put` (iter 222) | Composes iter 185 + iter 183 | Single-call bulk indexer over `SemanticStore`. Internally embeds the whole batch via `embed_documents_concurrent` (chunk-and-fan-out under Semaphore) and writes results to the underlying `Store` one at a time — far cheaper than calling `put` N times serially. Closes LangGraph's `BaseStore::mset` parity gap |
| `SemanticStore::bulk_delete` (iter 223) | Pair to bulk_put | Aligned `Vec<Result<bool>>` over `Store::delete`. Per-tenant namespace cleanup, retention sweeps, TTL boundary drops. Closes LangGraph's `BaseStore::mdelete` parity gap |
| `SemanticStore::bulk_get` (iter 224) | Third of the bulk trio | Aligned `Vec<Result<Option<(text, value)>>>` for known-key fetches. Distinct from `semantic_search` (which ranks by meaning). Surfaces corrupt-shape errors per key without tanking the whole batch. Closes the full `BaseStore::{mset, mdelete, mget}` parity |
| `ShutdownSignal` (iter 225) | `tokio::sync::Notify` + AtomicBool fired flag | Fifth distinct channel shape: N-waiter EDGE signal. Single `signal()` wakes every current and future waiter (idempotent); late `wait()` resolves instantly via the flag fast-path so no Notify-replay quirk. Channel-shape table now: mpsc / broadcast / watch / oneshot / Notify-edge |
| `until_shutdown` (iter 226) | `tokio::select!` over fut + ShutdownSignal::wait | Composable future combinator: `until_shutdown(model.invoke(...), &shutdown).await` returns `Some(T)` if the future completed, `None` if shutdown won. Inner future is dropped on shutdown so HTTP / DB / sleep resources are released promptly — no orphan in-flight work. Fast-path `is_signaled()` check skips polling the inner entirely if signal already fired |
| `batch_concurrent_with_shutdown` (iter 227) | Composes iter 182 + iter 225 | First parallel-batch ↔ coordination bridge. Distinct from wrapping `batch_concurrent` in `until_shutdown` (which discards everything if shutdown wins): preserves PARTIAL progress. Long eval batch finishes 60% before Ctrl+C → those 60% bank as `Ok`, remaining slots become `Err("cancelled by shutdown")`. Mechanical extension to the other 5 batch axes available |
| `embed_documents_concurrent_with_shutdown` (iter 228) | Composes iter 183 + iter 225 | Partial-progress preservation extended to the embeddings axis. Per-chunk granularity — bulk-indexer can flush only the `Ok` slots into a partial-but-valid embedding result on shutdown |
| `retrieve_concurrent_with_shutdown` (iter 229) | Composes iter 190 + iter 225 | Partial-progress preservation extended to the retriever axis. Three of six axes (chat / embed / retrieve) now bridge to the coordination primitives; tool / rerank / loader remain |
| `tool_dispatch_concurrent_with_shutdown` (iter 230) | Composes iter 191 + iter 225 | Partial-progress preservation extended to the heterogeneous tool axis. Four of six axes now bridge to coordination; rerank and loader remain. Real prod use: a Plan-and-Execute orchestrator with long-running tools banks completed tool results on cancel so agent context stays consistent |
| `rerank_concurrent_with_shutdown` (iter 231) | Composes iter 197 + iter 225 | Partial-progress preservation extended to the rerank axis. Five of six axes now bridge to coordination; only the loader axis remains. Long ensemble rerank runs over many (query, candidates) pairs bank completed `Ok` rerankings on Ctrl+C — eval reports stay computable on the partial result, remaining slots fill with `Err("cancelled by shutdown")` |
| `load_concurrent_with_shutdown` (iter 232) | Composes iter 187 + iter 225 | **Closes the bridge family.** Partial-progress preservation extended to the sixth and final parallel-batch axis (loader). All 6 axes (chat / embed / retriever / tool / rerank / loader) now bridge to `ShutdownSignal`. Real prod use: an ingestion job over hundreds of S3 keys + sitemap URLs banks completed loaders on Ctrl+C — half-loaded corpus stays valid, downstream embed/index stages can ship the partial result. Caveat: `Loader::load()` runs on `spawn_blocking`, so already-blocking OS threads finish their current call naturally — `abort_all()` only cancels the waiting-on-permit + join-await phases. Output slot still resolves to `Err("cancelled by shutdown")` once the await is dropped |
| `batch_concurrent_stream_with_shutdown` (iter 233) | Composes iter 210 + iter 225 | **Opens the second bridge family** (streaming + coordination), distinct from iter 227's Vec + coordination bridge. The streaming family already aborts on consumer-drop; this adds *producer-side* graceful end-of-interest. One central `ShutdownSignal` can stop multiple parallel batch streams without each consumer needing to drop its receiver — useful in orchestrators that own many sub-streams. Consumer sees a partial prefix in completion order, then the stream ends cleanly (no `Err`-filled tail). Mechanical extension to the other 5 stream axes (embed/retriever/tool/rerank/loader) available |
| `embed_documents_concurrent_stream_with_shutdown` (iter 234) | Composes iter 211 + iter 225 | Stream + coordination bridge extended to embed axis. Two of six stream axes (chat / embed) now bridge. Real prod use: a multi-collection bulk-ingestor running 5 parallel embed streams into 5 vector-store collections — a shared `ShutdownSignal` from a Ctrl+C / pod-shutdown handler ends every embed stream cleanly so each collection's already-flushed chunks stay valid |
| `retrieve_concurrent_stream_with_shutdown` (iter 235) | Composes iter 212 + iter 225 | Stream + coordination bridge extended to retriever axis. Three of six stream axes (chat / embed / retriever) now bridge. Real prod use: a long eval-harness streaming retrievals over thousands of queries — Ctrl+C fires the shared `ShutdownSignal`, every parallel retrieval stream terminates cleanly, drained results stay valid for a partial eval report |
| `tool_dispatch_concurrent_stream_with_shutdown` (iter 236) | Composes iter 213 + iter 225 | Stream + coordination bridge extended to tool axis. Four of six stream axes (chat / embed / retriever / tool) now bridge. Real prod use: a Plan-and-Execute orchestrator running a fan of tool calls per plan step — Ctrl+C fires the shared `ShutdownSignal`, every dispatch stream ends cleanly so accumulated tool-result state stays consistent with what's actually been executed (no half-applied side effects from aborted-mid-call tools) |
| `rerank_concurrent_stream_with_shutdown` (iter 237) | Composes iter 214 + iter 225 | Stream + coordination bridge extended to rerank axis. Five of six stream axes (chat / embed / retriever / tool / rerank) now bridge; only loader remains. Real prod use: a long ensemble eval streaming reranks over many `(query, candidates)` pairs — Ctrl+C fires the shared `ShutdownSignal`, every rerank stream terminates cleanly, drained results stay valid for a partial eval report |
| `load_concurrent_stream_with_shutdown` (iter 238) | Composes iter 215 + iter 225 | **Closes the stream + coordination bridge family.** Every one of the six parallel-batch axes (chat / embed / retriever / tool / rerank / loader) now exposes BOTH Vec + shutdown (iters 227-232) AND stream + shutdown (iters 233-238) variants. Real prod use: a multi-source ingestion crawler streaming docs from S3 + sitemap + GitHub + Confluence loaders into a single downstream chunker — Ctrl+C fires the shared `ShutdownSignal`, every loader stream ends cleanly, the partial corpus already drained into the chunker stays valid for a partial index. Same loader-axis caveat as iter 232: blocking `load()` threads finish their current call naturally; `abort_all` only cancels the waiting-on-permit + join-await phases |
| `Barrier` (iter 239) | `tokio::sync::Notify` + AtomicUsize counter + AtomicBool released-flag | **Sixth distinct channel shape**: wait-for-N rendezvous. After mpsc / broadcast / watch / oneshot / Notify-edge, the channel-shape table now closes a coordination gap: synchronized cohort start. `Barrier::new(n)` requires N participants to call `wait()`; the N-th arrival flips a released flag and `notify_waiters` wakes every pending sleeper. Late arrivals past N return instantly. The shutdown-aware `wait_with_shutdown(&shutdown)` returns `Some(())` on release / `None` on shutdown — pending waiters wake instead of parking forever when the orchestrator abandons a synchronized step. Distinct from `tokio::sync::Barrier`: that one provides only a non-shutdown `wait`; this version composes with the iter-225 coordination layer. Real prod use: coordinated multi-agent rounds (lockstep step-by-step execution), warm-up rendezvous (N workers each load model weights then start serving together), phase synchronization (pipeline stage N+1 can't begin until every item of stage N has finished) |
| `CountDownLatch` (iter 240) | `tokio::sync::Notify` + AtomicUsize counter (saturating-decrement) | **Sister to `Barrier` with asymmetric roles**: producers call `count_down()` (no wait) as work finishes, observers call `wait()` (no decrement) for the count to reach zero. The decoupling matters when producers and observers are different roles or modules — observer just needs a clone of the latch, doesn't need `JoinHandle`s or a `JoinSet` to track who's still running. Saturating-decrement via `compare_exchange` so extra `count_down()` calls past zero are no-ops (won't wrap). Same shutdown-aware `wait_with_shutdown(&shutdown)` semantics as `Barrier`: open-state wins over fired-shutdown (the work is done, no point reporting cancellation). Real prod use: fan-out completion gate (spawn N retrievers, await latch to know everyone returned regardless of who spawned them), initialization barrier (5 caches start filling, main task waits for all 5 to report ready before serving traffic), cleanup synchronization (every worker `count_down`s as it drains; supervisor waits for graceful exit) |
| `KeyedMutex<K>` (iter 241) | `parking_lot::Mutex<HashMap<K, Weak<tokio::sync::Mutex<()>>>>` | **Per-key async serialization**: different keys run in parallel; same-key callers queue in arrival order. Common prod pattern not directly in tokio: "only one task per `thread_id` mutating conversation state at a time" with thousands of threads independent. The `Weak` trick gives bounded memory: when no caller holds the lock and none are waiting, the inner `Arc<Mutex>` drops to refcount=0 → `Weak::upgrade` returns None → the next lookup creates a fresh `Mutex` for that key. `cleanup()` drops stale `Weak`s — cheap, safe to call from a periodic task; mostly matters for unbounded-key workloads (ephemeral request IDs / one-shot trace IDs) where stale entries would otherwise grow monotonically at ~16 bytes each. Real prod use: per-thread ReAct serialization, per-user tool-call coupling for state-mutating tools (Notion/GitHub/Jira), per-resource writer exclusivity (one upsert per vector-store collection at a time) |
| `RateLimiter` (iter 242) | `parking_lot::Mutex` over `(tokens: f64, last_refill: Instant)` + lazy-refill on every `acquire`/`try_acquire` | **Reusable async token-bucket** primitive distinct from `RateLimitedChatModel` (which wraps one ChatModel). Any caller — chat, embed, tool, loader — can charge against one shared budget. Useful when a single quota (e.g. one OpenAI key's TPM) covers heterogeneous calls. Lazy-refill design: no background task, each call recomputes the current token count from `last_refill_instant` (correct under arbitrary clock-tick patterns). Burst allowed up to `capacity`; sustained rate capped at `refill_per_sec`. Three modes: `try_acquire(n)` non-blocking; `acquire(n)` blocks until tokens accumulate; `acquire_with_shutdown(n, &shutdown)` cancellable. Critical detail: the shutdown path does NOT deduct tokens — the budget is preserved for surviving callers. Asks larger than `capacity` are clamped (otherwise would block forever, since the bucket can never accumulate more than capacity). Real prod use: shared provider quota across N agents (one OpenAI key serving 5 concurrent agents, every agent calls `limiter.acquire(tokens_estimate)` before its HTTP request), egress traffic shaping (outbound HTTP fan-out limited to N req/sec across all loaders), per-user fairness via the keyed-registry pattern from iter 241 (one limiter per `user_id` enforces "X requests per minute per user") |
| `CircuitBreaker` (iter 243) | `parking_lot::Mutex<InnerState>` (Closed{n_failures} / Open{until: Instant} / HalfOpenProbing) — `compare_exchange`-style state transitions inside the same lock guard | **Three-state resilience primitive** distinct from `RetryingChatModel`. RetryingChatModel retries on individual call errors (right when failures are *transient*); CircuitBreaker stops the bleeding when an upstream is *down* — after N consecutive failures, every subsequent call fails fast for `cooldown`, giving the upstream room to heal. Composable wrap-any-future API: `breaker.call(|| chat.invoke(msgs)).await`. Half-open semantics: exactly one in-flight probe allowed; concurrent callers see `CallError::CircuitOpen`; probe success → Closed (counter reset); probe failure → Open (cooldown reset). Manual `trip(cooldown)` / `reset()` hooks for ops runbooks ("we know provider X is down, open the breaker for 60s while we cut over"). Real prod use: third-party provider outage (after 5 consecutive 503s, breaker opens for 30s — agents fall back via `FallbackChatModel` instead of retrying for 30s each), vector-store quarantine (route reads to HNSW replica while pgvector is sick), tool blast-radius limiter (agent reasons about an unavailable tool rather than waiting on timeouts) |
| `CircuitBreakerChatModel` (iter 244) | `Arc<dyn ChatModel>` + `Arc<CircuitBreaker>`; map `CallError::CircuitOpen → Error::Provider("circuit breaker open")` so the existing fallback chain pattern-matches it as a transient | **Bridges the iter-243 primitive into the existing `ChatModel` resilience family**. Wraps any inner ChatModel; both `invoke` and `stream` admission-gate through the breaker. Streams are wrapped at the handshake (open → fail-fast without invoking inner stream); mid-stream failures stay the consumer's responsibility — matches the breaker's admission-control semantics. Stacks naturally with the existing toolkit; recommended outer-to-inner ordering: `CircuitBreakerChatModel` (fail-fast on persistent outage) → `FallbackChatModel` (switch provider on circuit-open) → `RetryingChatModel` (retry primary on transient errors) → `RateLimitedChatModel` (local rate cap) → real provider. The Error::Provider error message ensures `is_transient` doesn't accidentally retry circuit-open as a 5xx — the message text doesn't match the 5xx patterns |
| `CircuitBreakerEmbeddings` (iter 245) | `Arc<dyn Embeddings>` + `Arc<CircuitBreaker>` (one shared breaker spans both `embed_query` and `embed_documents`) | **Embed-axis mirror of iter 244**. The shared-breaker design matters: `embed_query` and `embed_documents` typically hit the same upstream; one provider going sick should fail-fast both call shapes. A flapping query path opens the breaker for document indexing (verified by test). Same composition pattern as the chat side — stack with `FallbackEmbeddings` for circuit-open → secondary embedder. With this iter the resilience matrix is symmetric across chat/embed: every chat resilience wrapper (Retrying / RateLimited / Fallback / Race / Timeout / CircuitBreaker) has its embedding sibling |
| `SharedRateLimited{ChatModel, Embeddings}` (iter 246) | `Arc<dyn ChatModel>` / `Arc<dyn Embeddings>` + `Arc<RateLimiter>` (iter-242 primitive) | **Bridges iter-242 `RateLimiter` into the resilience family**. Distinct from the existing `RateLimitedChatModel` / `RateLimitedEmbeddings` which own their own bucket per wrapper: this variant takes a shared `Arc<RateLimiter>` so multiple wrappers share ONE budget. The realistic prod pattern: one provider API key has ONE TPM/RPM quota covering several model variants (gpt-4 / gpt-4-turbo / gpt-4o-mini all on one key). With per-wrapper buckets the aggregate exceeds the real budget; with this variant they all charge against one limiter. The shared bucket can also span the chat/embed axes — useful when one OpenAI key covers both completions AND embeddings APIs. Each call charges 1 token at handshake; users wanting token-weighted accounting can call `limiter.acquire(estimate).await` directly upstream of the wrapped model. Verified by tests including a 4-call burst across two distinct wrapped models that must take >= 30ms with shared cap=2/refill=50/sec |
| `KeyedSerializedChatModel` (iter 247) | `Arc<dyn ChatModel>` + `Arc<KeyedMutex<String>>` (iter-241 primitive) + `Arc<dyn Fn(&[Message], &ChatOptions) -> Option<String>>` key extractor | **Bridges iter-241 `KeyedMutex` into the chat family**. The realistic prod use case: a stateful agent's ReAct loop reads-then-writes shared conversation state per `thread_id`. Two concurrent steps for the same thread interleave their reads/writes and corrupt state. A single global mutex fixes correctness but serializes every thread one-at-a-time. This wrapper lifts the per-thread ReAct-step lock into the model layer: same `thread_id` queues, different `thread_id`s run concurrently. Key extractor returns `Option<String>` — `None` means "no serialization for this call" (passes through unlocked), useful for one-shot calls without thread context. Streaming: the lock is held only across the handshake (the inner `stream()` call); it releases when this method returns since the caller drives the stream itself. For per-step exclusion across the whole stream lifetime, hold the lock externally around the `stream` + `drain` cycle |
| `Bulkhead` (iter 248) | `Arc<tokio::sync::Semaphore>` + `AtomicU64` rejected counter | **Concurrent-call cap with REJECTION semantics**, named after the "Release It!" pattern (separate failure domains so one saturated dependency doesn't drown the process). Distinct from a plain Semaphore: where Semaphore queues callers indefinitely, a Bulkhead surfaces the saturation as a *signal* via `try_enter() -> Option<BulkheadGuard>`. Three modes: `try_enter` (non-blocking, instant reject if at cap), `enter` (block until slot opens — Semaphore-equivalent, provided so the wrapper is useful when the caller actually wants queueing), `enter_with_timeout(t)` (block up to `t` then reject). Tracks `rejected_count` and `in_flight()` for telemetry. The real value vs `Arc<Semaphore>` is the *intent contract* — the type name documents that callers are expected to handle rejection rather than wait forever. Real prod use: per-tool concurrent cap (5 in-flight max for flaky API; the 6th caller gets BulkheadFull so the agent picks a different action rather than waiting), vector-store connection budget (cap retrievers below pool size to leave headroom for writes), outbound HTTP fan-out cap (skip the (N+1)th slow request rather than queue forever) |
| `BulkheadChatModel` + `BulkheadEmbeddings` (iter 249) | `Arc<dyn ChatModel>` / `Arc<dyn Embeddings>` + `Arc<Bulkhead>` (iter-248 primitive) + `BulkheadMode::{Reject, WaitUpTo(Duration)}` | **Bridges iter-248 into the resilience family**. Two modes per wrapper (configurable at construction): `Reject` for instant rejection on cap (the typical bulkhead use), `WaitUpTo(t)` for block-then-reject with a deadline. The critical design choice — bulkhead-full surfaces as `Error::RateLimited { retry_after_ms: None }`, NOT a custom error variant. Reason: `is_transient` already matches `RateLimited`, so `RetryingChatModel` retries with backoff (slot may have opened) and `FallbackChatModel` switches provider — both interactions just work, no extra wiring. One `Arc<Bulkhead>` can span multiple wrappers AND across chat+embed axes, enforcing one cap over a shared resource budget (verified by the chat-and-embed-share-one-budget test). With this iter, every base coordination/resilience primitive that makes sense as a single-call wrapper has its ChatModel/Embeddings sibling: CircuitBreaker (244/245), SharedRateLimiter (246), KeyedMutex (247-chat-only), Bulkhead (249). The Barrier and CountDownLatch primitives remain unwrapped — they're cohort-coordination shapes, not single-call patterns |
| `hedged_call(primary, backup, hedge_delay)` (iter 250) | `tokio::pin!` + `tokio::select! { biased; primary; sleep(hedge_delay) }` for phase 1; `tokio::select! { primary; backup }` for phase 2 | **Tail-latency mitigation combinator** distinct from iter-184 `RaceChatModel`. Race issues to *every* inner provider simultaneously (right when "first response wins, cost-no-object"). Hedge issues primary alone for `hedge_delay`; only if primary hasn't finished does backup also run. Fast-path requests pay zero overhead — only the slow tail incurs the second-call cost. Standard pattern from Dean & Barroso 2013 ("The Tail At Scale"). Implementation detail: phase-1 select uses `biased` so primary wins the race-to-flag if both happen to be ready in the same tick (cheap correctness guard against a rare scheduling artifact). Loser future is dropped at end-of-`select!`, releasing whatever HTTP / DB / sleep it held via tokio cancellation. Generic over future output type, so it composes with `Result<_, _>` (the natural shape for hedging fallible operations). Real prod use: LLM provider with 500ms p50 / 30s p99 — set `hedge_delay = 2s`; calls under 2s pay nothing, slow tail covered by backup. Multi-region failover: primary in us-east-1, backup in us-west-2; hedge after 1s |
| `HedgedChatModel` + `HedgedEmbeddings` (iter 251) | Two `Arc<dyn ChatModel>` (or `Embeddings`) + `Duration` hedge delay; delegates to iter-250 `hedged_call` | **Bridges iter-250 into the resilience family**. Distinct from iter-184 `RaceChatModel`: race issues to both providers simultaneously (always doubles cost); hedge only pays the second-call cost when primary is slow. The right trade-off when median latency is fine and you only want to insure against the p99. Embed axis: both `embed_query` and `embed_documents` hedged. Streaming caveat: `stream()` is primary-only — token streams can't be cleanly raced (chunks can't merge or be chosen between mid-stream); callers needing stream tail-latency mitigation should run their own per-chunk timeout + restart-on-different-provider logic. With this iter, every "operate over a single call" coordination/resilience primitive (CircuitBreaker, SharedRateLimiter, KeyedMutex, Bulkhead, Hedged) has its ChatModel + Embeddings sibling — the resilience matrix is fully populated |
| `Singleflight<K, V>` (iter 252) | `parking_lot::Mutex<HashMap<K, broadcast::Sender<V>>>` + leader/follower state machine in `get_or_compute` | **Request-coalescing primitive** — same shape as Go's `golang.org/x/sync/singleflight`. When N concurrent callers ask for the same key, only ONE inner computation runs; followers subscribe to the leader's `tokio::sync::broadcast` channel and receive the cloned result. The map entry is removed AFTER the leader finishes so a fresh `get_or_compute` call starts a new compute (no stale-cache problem). Distinct from `KeyedMutex` (iter 241): KeyedMutex serializes same-key callers — each one runs its own compute one-at-a-time. Singleflight runs the compute *once* and broadcasts. `V: Clone` is required because `broadcast::Sender::send` clones to each receiver; use `Arc<T>` for expensive types so the broadcast is just an Arc-clone. Edge cases: leader cancellation (channel closed without send) → followers fall back to running their own compute; channel-Lagged (shouldn't happen with capacity=1 single-broadcast but handled defensively the same way). Real prod use: cache-miss thundering herd (100 agents want the same embedding → 1 HTTP call, 100 results), idempotent-tool coalescing (10 concurrent stock-price-for-AAPL calls → 1 upstream), lazy model-weights initialization (5 workers want the same weights → 1 load) |
| `SingleflightEmbeddings` (iter 253) | `Arc<dyn Embeddings>` + `Arc<Singleflight<String, Arc<Result<Vec<f32>, String>>>>` | **Bridges iter-252 into embeddings**. N concurrent `embed_query("foo")` calls share ONE inner HTTP call; followers get the leader's result via `tokio::sync::broadcast`. The `Arc<Result<Vec<f32>, String>>` type is forced by Singleflight's `V: Clone` constraint — `litgraph_core::Error` isn't Clone, so error variants are stringified into `Error::Provider(s)` on the caller side. Lossy by design: keeping exact error variants would require `Arc<Error>`, but converting back via `try_unwrap` is racy with multiple receivers (always fails when ≥1 follower is still around) so stringification is the practical choice. Documented in the wrapper's rustdoc; callers needing exact error variants should not coalesce. `embed_documents` passes through unchanged — multi-doc batches rarely repeat exactly, coalescing them would need a hashable key over `Vec<String>` for marginal expected win. With this iter, the singleflight pattern joins the resilience matrix on the embeddings axis (chat axis remains un-coalesced — system prompts are typically distinct per call, and dedup of model invocations changes semantics in a way users shouldn't get for free) |
| `RecordingChatModel` + `ReplayingChatModel` + `Cassette` (iter 254) | `parking_lot::Mutex<Cassette>` shared between recorder and the test scope; cassette serde-JSON; blake3 over canonical JSON of `(messages, opts)` for hash key | **VCR-style record/replay for agent tests** — substantial feature missing from most LangChain alternatives. Pivot from the small-primitive cadence to a real prod-grade testing-infra capability. Use case flow: developer wraps their live model in `RecordingChatModel` during a one-time real-traffic test run; serializes the resulting `Cassette` to disk (a JSON file in `tests/cassettes/`); CI runs use `ReplayingChatModel::new(loaded_cassette, None)` for deterministic, no-API-cost, no-quota-burn replay. Optional `passthrough: Option<Arc<dyn ChatModel>>` for "record-then-fill-gaps" workflows: cassette miss falls through to the live model. Hash determinism guaranteed by canonical-JSON serialization of all serde-derived request fields (no HashMap fields in messages or opts), so test cassettes are stable across rebuilds. Streaming intentionally not recorded — replay of token streams without preserving inter-chunk timing is misleading; stream calls pass through. Required adding `serde`, `serde_json`, `blake3` as runtime deps to litgraph-resilience (previously dev-only). Composes cleanly with the rest of the resilience family — wrap a recorder *outside* a retry/breaker chain to capture the actual user-visible request/response pairs, or *inside* to capture the underlying provider behavior |
| `Cassette::{load,save}_from_file` + `RecordingEmbeddings` + `ReplayingEmbeddings` + `EmbedCassette` + `EmbedExchange::{Query, Documents}` (iter 255) | `serde_json::to_string_pretty` + `std::fs` for IO; `EmbedExchange` is a tagged-union serde enum so one cassette covers both `embed_query` and `embed_documents` recordings | **Closes the record/replay workflow.** File IO: `Cassette::save_to_file(path)` creates parent directories with `create_dir_all` and writes pretty JSON; `Cassette::load_from_file(path)` reads + parses. Errors surface as `Error::Other(...)`. Embeddings axis: same recorder/replayer pattern with `EmbedCassette` (a tagged-union enum so query/documents responses share one cassette file). `embed_query_hash(text)` is blake3 over the text bytes; `embed_documents_hash(texts)` is blake3 over canonical JSON of the `Vec<String>` (order-sensitive — embed_documents returns aligned vectors so reordering changes the request semantically). `ReplayingEmbeddings::dimensions()` defaults to the first exchange's vector length when no passthrough is set, otherwise delegates to passthrough — keeps `dimensions()` correct for downstream code that consults it pre-call. Real prod use: `cargo test` in CI runs the full agent flow (chat + embed + retrieval) against committed cassettes, no API keys required, response stable across runs. With this iter the record/replay story is symmetric across chat/embed and end-to-end usable; the only major missing piece (chunk-timing-aware streaming replay) remains documented as out-of-scope |
| `RecordingTool` + `ReplayingTool` + `ToolCassette` + `tool_args_hash` (iter 256) | Same machinery as iter 254/255 but over `litgraph_core::tool::Tool`. `ToolExchange { request_hash, tool_name, args, response }` with both args and response as `serde_json::Value` for full agent-tool fidelity | **Third record/replay axis** (chat / embed / tool). Hash key: blake3 over canonical JSON of `args`. The `ReplayingTool` schema-handling deserves a note: when no passthrough is configured, the schema is synthesized from a configurable `name` + `description` (`with_name` / `with_description` builders) plus a minimal `{"type": "object"}` parameters object. That's enough to satisfy callers that only need the cassette's responses (typical agent-test setup); when full schema fidelity is needed, the optional passthrough's schema is proxied. Builder pattern keeps the API ergonomic for the common test setup of `ReplayingTool::new(cass, None).with_name("search")`. Real prod use: agent integration tests that exercise external-API tools — record against a staging environment once, commit the cassette to `tests/cassettes/`, replay in CI without any service dependencies (no Slack, no GitHub, no real database hits). With this iter the record/replay matrix is complete across the three primary axes — combined with iter 254/255 file IO, the entire agent loop (LLM calls + embeddings + tool calls) is now CI-deterministic with one consistent VCR-style API |
| `RssAtomLoader` (iter 257) | `quick-xml` pull parser walking a single state machine over both RSS 2.0 and Atom 1.0 inputs; feature-gated `rss` (re-uses the existing `quick-xml` optional dep that was already pulled in by the `docx` feature) | **Pivot from the resilience cadence to a new loader.** RSS/Atom is the canonical way blogs / news / podcast / release-note feeds publish a stream of recent items; the framework already had 24+ loaders but this canonical one was missing. Single parser handles both formats: namespace-prefix-stripped tag matching (`content:encoded` → `encoded`) so the same match arms work for either schema, attribute-based `<link href="…"/>` (Atom) and text-content `<link>https://…</link>` (RSS) both supported, body-resolution priority `content > summary > title` so title-only items still emit a non-empty document. `pubDate` (RFC822) is best-effort normalized to RFC3339. Per-item metadata: `title`, `link`, `published`, `feed_title`, `feed_url`. Configurable: `with_max_items` cap to bound feed-bomb risk, `with_skip_empty` filter, `with_user_agent` / `with_timeout` knobs (matches the convention from `SitemapLoader` etc). The `parse(&self, xml: &str) -> Vec<FeedItem>` method is `pub` so tests can drive the parser directly without reqwest — keeping the unit-test layer cheap and offline. Real prod use: news brief generator (ingest top tech-blog feeds, agent summarizes the last 24h), release-notes RAG (ingest a project's `releases.atom`, agent answers "what changed in v3.x?"), competitive monitoring (cluster competitor blog feeds for theme detection) |
| `HackerNewsLoader` (iter 258) | Public HN Firebase API at `hacker-news.firebaseio.com/v0/` — no auth, no rate-limit headers; HN explicitly invites traffic. Two-step fetch: `<feed>stories.json` returns IDs (~500 max), then `item/<id>.json` per item. Rayon-parallel item fetch since per-item calls are independent | **Second loader pivot in this iter cluster.** Six feed sources via `HnFeed::{Top, New, Best, Ask, Show, Job}`. Each item produces a Document with the title and text combined (for Ask/Show posts where the body content is the actual prose) and metadata `{hn_id, title, url, by, score, time, type, descendants, feed}`. Items lacking both title and text are dropped silently — story-deletion / dead-account markers shouldn't pollute the corpus. The loader is offline-testable: `HnItem::into_document(feed_name)` is public, so tests drive the parser without reqwest. `with_base_url` knob lets integration tests point at a fake-HN local server. Default `max_items=30` to keep the typical fetch bounded; bump for batch-ingestion runs. Real prod use: tech-news brief, title-only zero-shot embedding corpus (cheap to build, useful for testing semantic search), Show-HN trend monitoring over time |
| `PriorityQueue<T>` (iter 259) | `parking_lot::Mutex<BinaryHeap<Entry<T>>>` + `AtomicU64` insertion sequence + `tokio::sync::Notify` for waking poppers. `Entry: Ord` orders by priority desc, then seq asc | **Async work queue with priority-based pop**, distinct from `tokio::sync::mpsc` (FIFO across the whole channel — a high-priority task pushed late waits behind every earlier-pushed task). Within the same priority, FIFO order is preserved via an insertion sequence number. Three API shapes: `try_pop` non-blocking, `pop` blocking with `Notify::notified` race-safe pattern (register before re-check, same as `ShutdownSignal::wait`), `pop_with_shutdown` for cancellable graceful drain. The `Send + 'static` bound on `T` is intentional — items must be safe to move across task boundaries since the queue is typically shared via `Arc<PriorityQueue<T>>`. Builder-free API by design: most prod uses set the priority per-push at the call site rather than via wrapper config. Real prod use: urgent retries first (a graph node that failed and is being re-scheduled jumps ahead of fresh work), hard-cases-first eval harness (most-likely-to-fail rows scored first so cancelled runs surface failures fastest), latency-budget UI requests (`priority=10` jumps the batch). Pairs naturally with iter-248 `Bulkhead` (concurrent-cap on top of priority) and iter-225 `ShutdownSignal` (graceful drain via `pop_with_shutdown`) |
| `MetricsRegistry` + `Counter` + `Gauge` + `Histogram` (iter 260) | `parking_lot::RwLock<HashMap<String, Arc<…>>>` for the registry; atomic hot-path on every primitive. Counter = `AtomicU64`. Gauge = `AtomicI64` (signed because gauges go down too). Histogram = `Vec<AtomicU64>` per bucket + `AtomicU64` for sum-bits (f64 stored as `to_bits()`, mutated via CAS-loop to keep the hot path lock-free) + `AtomicU64` for count | **In-process metrics aggregation** — a foundational observability primitive distinct from the tracing/OTel layer. The framework already had `tracing` spans + OTLP exporter + CostTracker, but no general "count this across N tasks cheaply" primitive. `MetricsRegistry` fills that gap. Get-or-create lookup so multiple call sites share one Counter/Gauge/Histogram by name. `to_prometheus()` renders text exposition format suitable for direct response from a `/metrics` HTTP handler. Bucket-bounds for histograms: caller-supplied; auto-sorted ascending and deduped on construction; `+Inf` bucket appended automatically per Prometheus convention. Sum is updated via `compare_exchange_weak` CAS on f64-bits — adds non-trivially-aligned-to-atomic-load f64 work without a Mutex. Name sanitization on Prometheus export rewrites disallowed chars (`-`, `.`) to `_` per the Prometheus naming spec. Distinct from tracing: tracing answers "what did this one request do?", metrics answers "what's the rate of X across the whole process right now?". Both useful; they answer different questions. Real prod use: agent loop tool-call counters via `r.counter("tool_calls_total").inc()`, in-flight gauges via wrapper that calls `g.inc()` on entry / `g.dec()` on exit, latency histogram with `[0.005, 0.01, 0.025, …]` buckets and `h.observe(elapsed.as_secs_f64())` |
| `MetricsChatModel` + `MetricsEmbeddings` (iter 261) | `Arc<dyn ChatModel>` / `Arc<dyn Embeddings>` + pre-resolved `MetricsHandles { invocations, errors, in_flight, latency }` (Counter/Gauge/Histogram instances captured at construction so the hot path is pure atomic ops, no HashMap lookup) + `InFlightGuard` RAII type that decs the gauge on drop (so cancellation/panic paths still leave the gauge correct) | **Bridges iter-260 into the resilience family.** Auto-instruments any wrapped model with the four standard metrics: `<prefix>_invocations_total`, `<prefix>_errors_total`, `<prefix>_in_flight`, `<prefix>_latency_seconds`. Default prefix `chat` for chat / `embed` for embed; `with_prefix(name, registry, "openai_gpt4")` for per-model labeling so multiple wrapped models register distinct metric series; `with_buckets(...)` for custom histogram buckets when default `[0.005..30]` doesn't fit the workload. The `DEFAULT_LATENCY_BUCKETS_SECS` constant is exported so callers can build on it. Streaming: metrics recorded at the *handshake* (the time until `stream()` returns its outer Result) — per-token timing is the consumer's responsibility. Composes with the rest of the resilience matrix: wrap inside-out for "metrics on the user-visible API → retries → rate-limit → real provider" by putting `MetricsChatModel` outermost. Real prod use: drop-in `/metrics` endpoint for any litGraph deployment — wrap every provider once at startup, expose `registry.to_prometheus()` from an axum handler |
| `MetricsTool` (iter 262) | Same `MetricsHandles` + `InFlightGuard` machinery as iter 261, over `litgraph_core::tool::Tool`. Default prefix derived from the inner tool's `name()`, sanitized via the same allowed-character set as `MetricsRegistry::to_prometheus` (`[a-zA-Z_][a-zA-Z0-9_:]*`) | **Third axis of metrics auto-instrumentation** (chat / embed / tool). Closes the metrics matrix across the same three primary axes covered by iter 254-256 record/replay. Default-prefix-from-tool-name design choice: most agent setups have many distinct tools but each tool's behavior is what's interesting per metric series. Auto-naming via the tool's schema name (with sanitization for Prometheus rules — `http.get` → `http_get`) means callers don't have to specify a prefix at every wrap site; the default already gives per-tool metric series. `with_prefix` for explicit labeling when multiple instances of the same tool need distinct series (e.g., `http_get_external` vs `http_get_internal`). Real prod use: agent debugging is largely "which tool failed?" — the tool axis is where most actionable signal lives. With this iter the `/metrics` endpoint shows per-tool error rates, latency P99, and concurrent-call counts out-of-the-box |
| `SingleflightTool` (iter 263) | `Arc<dyn Tool>` + `Arc<Singleflight<String, Arc<Result<Value, String>>>>`; hash key is blake3 over canonical JSON of args (same hash function as iter-256 `tool_args_hash`) | **Closes the request-coalescing matrix on the tool axis.** Family across the three primary axes: chat (intentionally not coalesced — collapsing model invocations changes user-visible semantics, and stochastic outputs would silently merge), embed (iter 253), tool (this iter). N concurrent identical `run(args)` calls share ONE upstream invocation; followers subscribe to the leader's `tokio::sync::broadcast` and receive the cloned result. **Critical safety contract: must NOT wrap tools with side effects** — coalescing collapses N intent-distinct calls into one execution, which deduplicates the side effects (write, send, mutate). Documented in the wrapper rustdoc with explicit "When NOT to coalesce" callout. Idempotent reads / pure-function tools are the safe wrap targets. Errors lossy-stringify to `Error::Provider(s)` (same trade-off as iter 253) so the broadcast value type stays Clone. Real prod use: `lookup_user("alice")` called 10× concurrently from different agent steps → 1 DB round-trip; hot search query coalescing; stable function tools (currency conversion, time-zone arithmetic) where output is purely a function of args |
| `BitbucketIssuesLoader` (iter 264) | Bitbucket Cloud REST API v2 (`api.bitbucket.org/2.0/`); supports both Basic auth (username + app password, default) and OAuth bearer; cursor pagination via the API's `next` URL field; BBQL filter via `q=` query param | **Third Git provider in the loader trio**, after GitHub (iter 41-42 issues + files) and GitLab (iter 149/154). Bitbucket's filter syntax (BBQL) is more expressive than GitHub's flat query params: `state="open" AND kind="bug"` joins multiple predicates with `AND` operator. The wrapper exposes the common axes (`with_state`, `with_kind`, `with_max_issues`, `with_include_comments`) and translates GitLab-style `"opened"` → `"open"` for cross-provider portability. Per-issue metadata captures Bitbucket's full schema: `{workspace, repo, issue_id, state, kind, priority, assignee, reporter, created_on, updated_on, votes, watches, link}`. The optional `with_include_comments(true)` flag appends the comment thread (`@user: body`) so the resulting Document covers the full discussion — useful for issue-tracker RAG where the body alone isn't enough context. Offline-testable: `pub fn issue_to_document(&self, &Value, &[Value]) -> Option<Document>` lets unit tests drive the parser with hand-crafted JSON fixtures; no live API needed for the test layer. With this iter, the framework's GitHub-parity story across the three big Git hosts is complete |
| `BitbucketFilesLoader` (iter 265) | Same auth machinery as iter 264 over `/src/{ref}/{path}` endpoint; behavior depends on path target — directory yields JSON listing with `commit_directory` / `commit_file` entries, file yields raw text in body. BFS walk via a `Vec<(path, depth)>` work-queue with cursor pagination per directory level | **Closes the Git-host loader matrix.** With this iter all of `{GitHub, GitLab, Bitbucket} × {Issues, Files}` is populated. Walks the repo source tree from `with_root_path` (default repo root) breadth-first up to `with_max_depth` (default 32). Per-listing pagination: Bitbucket returns `{values, next}`; the loader chases `next` links until exhaustion before moving to the next directory. Files filtered by `with_extensions` (allowlist, case-insensitive, dot-normalized) and `with_exclude_paths` (substring denylist; defaults `node_modules/`, `target/`, `.git/`, lockfiles, minified bundles — sane out-of-the-box for typical repos). `with_max_file_size_bytes` (default 1 MiB) skips blobs masquerading as text without downloading them, since Bitbucket includes `size` in the directory listing. Per-file Document id `bitbucket:{ws}/{repo}@{ref}#{path}` — distinct from the issue id format so a single corpus can mix sources. Offline-testable: `passes_filters(path)` and `file_to_document(path, content)` are public, allowing the test layer to verify all filter logic without HTTP |
| `RagFusionRetriever` (iter 266) | LLM `generate_queries(query) -> Vec<String>` paraphrase generation (same pattern as iter-132 MultiQueryRetriever) + per-query parallel `JoinSet<retrieve(q, branch_k)>` + `rrf_fuse(branches, k_rrf, top_k)` from the existing iter-30-era hybrid module | **Pivot to retrieval — fresh primitive, not yet another wrapper.** Distinct from `MultiQueryRetriever`: that paraphrases-then-dedups-by-id-with-first-occurrence-wins, preserving the original query's per-query ranking. RAG-Fusion (Raudaschl 2023; uses Cormack-Croft-Smucker 2009 reciprocal-rank fusion under the hood) instead **scores by aggregate rank position across all branches** — a doc ranked #2 in branch A and #1 in branch B beats a doc ranked #1 in branch A but absent in B. The technique is the right pick when *consistency across paraphrases* matters more than respecting the original query's exact ordering. Knobs: `with_num_variations(n)` (default 4), `with_rrf_k(k)` (default 60 from Cormack), `with_branch_k(n)` per-query depth (default 10) so RRF has more candidates to score, `with_include_original(b)` keeps the user's literal phrasing alongside paraphrases, `with_system_prompt` for prompt customization. Falls back to literal query when LLM returns empty paraphrases — degrades gracefully instead of returning empty. Surfaces aggregated branch errors only if EVERY branch failed (partial errors are silently dropped to keep retrieval resilient to single-branch flakes). With this iter the retrieval roster grows to: Vector / BM25 / Hybrid / Reranking / ParentDocument / MultiQuery / RagFusion / ContextualCompression / SelfQuery / TimeWeighted / HyDE / MMR / Ensemble / MultiVector |
| `StepBackRetriever` (iter 267) | LLM `generate_step_back(query) -> String` produces ONE more-abstract question; per-query parallel `JoinSet<retrieve(q, branch_k)>` over original + step-back; `HashSet<id>` dedup with original-branch-first ordering | **Step-back prompting** (Zheng et al. 2023, Google Research) adapted for retrieval. Distinct from MultiQuery (paraphrases at the SAME abstraction level), RagFusion (paraphrases + RRF), and HyDE (hypothetical document). Step-back asks the LLM to generate a *more abstract / higher-level* question that captures the underlying concept. Example from the paper: "Was Estella M. Leopold a member of any organization?" → "What is the personal history of Estella M. Leopold?". The retriever then queries with BOTH and unions the results — useful for highly specific factual queries that may not match any document title verbatim but live inside more general background docs. Implementation includes the paper's few-shot examples by default (`with_include_examples(false)` to omit them for token cost). Skips the step-back branch when the LLM echoes the original query (degenerate case where the question is already general enough). Output cleaning handles common LLM artifacts: prefix `"Step-back Question:"`, wrapping quotes, multi-line responses (takes first line only). Cost: 1 LLM call + 2× base retrieval cost in parallel. Retrieval roster grows to 15 |
| `SubQueryRetriever` (iter 268) | LLM `decompose(query) -> Vec<String>` splits compound queries into atomic sub-questions; per-sub-query parallel `JoinSet<retrieve(q, branch_k)>`; round-robin merge across branches before final dedup-by-id + truncate to `k` | **Compound-query decomposition** — fifth and final member of the query-expansion family. Distinct from the rest by axis: MultiQuery / RagFusion paraphrase at the same level (single intent, varied wording); StepBack abstracts upward (one more general question); HyDE generates a hypothetical document; SubQuery **splits multi-intent**. Real prod use: comparison queries ("Compare X vs Y" where the corpus has separate docs about X and Y but no doc about both), multi-entity lookups, conjunction queries ("Find papers that cite both Z1 and Z2"). The round-robin merge step is the key ranking choice: branch_a[0], branch_b[0], branch_a[1], branch_b[1], … rather than concat — so per-sub-query top-1 results interleave at the head of the output list rather than one branch dominating. The system prompt tells the LLM "if the question is already atomic, output the original unchanged" so single-intent queries degrade to a single-branch retrieval. `with_include_original(true)` for the rare case where the compound phrasing itself matches a useful doc. With this iter the retrieval roster grows to 16, and the query-expansion family is complete across the four expansion axes (paraphrase / fuse / abstract / split) |
| `MarkdownTableSplitter` (iter 269) | Line-by-line walk over the markdown; GFM table detection (`\|...\|` row + `\|---\|` separator + N data rows); table emitted as a single chunk; non-table prose buffered and emitted via an inner `Splitter` (default `RecursiveCharacterSplitter` with chunk_size 1000, overlap 200) | **Pivot to splitters.** Real prod problem the existing splitters didn't solve: vector retrieval over docs with markdown tables fragments tables mid-row, which destroys table comprehension downstream. The naive recursive-char splitter sees a table the same as prose and chops by char count; the embedding model then sees a header row in chunk N, a separator row in chunk N+1, and orphan data rows in chunk N+2 — none of which individually convey the table's meaning. This iter's splitter walks line-by-line, detects GFM tables (header row + separator row pattern is the strong signal), and emits each table block atomically. Recognizes alignment separators (`:---`, `---:`, `:---:`). Borderless / "compact" tables without the separator row are intentionally NOT recognized as tables since the signal is ambiguous — that flavor is left to `MarkdownHeaderSplitter`. Optional `with_max_table_chars(Some(n))` enables row-wise fragmentation when a single table exceeds `n` chars, preserving the header + separator on every fragment so downstream retrieval still gets table context. Default `None` keeps tables atomic regardless of size — losing table integrity is usually worse than emitting a long chunk |
| `CsvRowSplitter` (iter 270) | Char-by-char row walker that tracks an in-quote flag (RFC-4180 `"..."` cells with literal `\n` stay intact); chunks data rows by configurable `rows_per_chunk` (default 100); prefixes the header row to every chunk so columns stay interpretable downstream | **Sibling to iter-269 `MarkdownTableSplitter` — same prod gap on the CSV axis.** Existing recursive-char/token splitters break CSV rows mid-cell; downstream embedding sees `Alice,2024-01-15,$10` followed by `0,paid,Bob,2024-01-16,$200,unpaid` — the comma-position semantics are destroyed. This splitter chunks by row count (semantic unit for tabular data) and replicates the header on every chunk. Quoted-field-with-newline handling matches Python `csv` and Postgres `COPY`: the row walker only ends a row at a newline that lands outside any `"..."` quoted field. CRLF line endings normalized to LF in output (Excel-style CSVs round-trip cleanly). `with_header(false)` skips the header-prefix behavior for headerless CSVs (each line is a row, every chunk emits N rows verbatim). Default `rows_per_chunk = 100` — small enough that a chunk fits in any embedding context, large enough that table-summary-style retrieval gets useful context |
| `TimeoutRetriever` (iter 271) | `Arc<dyn Retriever>` + `Duration` deadline; `tokio::time::timeout` over `inner.retrieve(...)` | **Closes another corner of the resilience matrix.** Mirrors iter-194 `TimeoutChatModel` / `TimeoutEmbeddings` for the retriever axis. Real prod use: vector stores with unbounded p99 latency under load (HNSW with cold pages, pgvector under heavy write contention) — interactive UI can't wait 30s for a slow retrieval. Wrap with a 2s deadline; agent falls through to backup retriever or "no context" answer. Choice of `Error::Timeout` as the timeout outcome means the existing `is_transient` classifier in `RetryingChatModel` treats this as retryable; wrapping `RetryingChatModel` around code that uses a `TimeoutRetriever` results in automatic retry-with-backoff on timeout, matching the chat/embed axes. Lives in `litgraph-retrieval` (not `litgraph-resilience`) because a `Retriever` wrapper that depends on resilience would create a circular dep with retrieval — the choice is consistent with how `RerankingRetriever` etc also live in the retrieval crate. With this iter the timeout-wrapper trio is complete: chat (iter 194), embed (iter 194), retriever (this iter) |
| `RetryingRetriever` (iter 272) | `Arc<dyn Retriever>` + `RetryConfig`; `backon::Retryable` with `ExponentialBuilder` (min/max/factor/max_times/jitter); inline `is_transient(&Error)` classifier matching the resilience-crate version | **Closes the retry-wrapper trio** (chat/embed/retriever). The retriever axis was the missing piece. Pairs naturally with iter-271 `TimeoutRetriever`: `RetryingRetriever(TimeoutRetriever(inner))` is the standard prod pattern for vector stores that occasionally exceed per-call deadlines but recover on retry. The classifier retries `RateLimited` (managed vector stores throttling under load), `Timeout` (composes with iter 271), and `Provider(s)` where `s` matches a 5xx / connection-reset pattern. Terminal errors (`InvalidInput`, parse errors) NOT retried — replays just waste cycles. Default config matches `RetryingChatModel` for consistency: 5 attempts, 200ms→10s exponential with 2× factor + jitter. Required adding `backon` as a runtime dep to litgraph-retrieval (already in workspace via the resilience crate). With this iter every chat/embed resilience wrapper that makes sense for retrieval has its sibling: Timeout (271), Retrying (this iter). Other resilience wrappers don't apply directly to retrieval (rate limiting belongs at the vector-store API level, not the retriever level; circuit breaking on retrieval is fine but the impl is identical to chat-side and not yet shipped) |
| `MetricsRetriever` (iter 273) | Same `Handles { invocations, errors, in_flight, latency }` + `InFlightGuard` RAII machinery as iter 261 (chat) / 262 (tool); pre-resolved at construction so the hot path is pure atomic ops; default histogram buckets are tighter than chat/embed (`[0.001, 0.0025, …, 10.0]` seconds) | **Closes the metrics-instrumentation matrix.** Coverage now spans all four primary axes: chat (261), embed (261), tool (262), retrieve (this iter). With one wrap call at startup any `Arc<dyn Retriever>` becomes observable on the same `/metrics` endpoint that already covers chat / embed / tool. Tighter default histogram buckets reflect that retrieval is typically faster than LLM calls — the geometric spread starts at 1ms (vs 5ms for chat) so HNSW / pgvector / BM25 latency distributions show meaningful detail. Default prefix `retrieve` for the common case; `with_prefix("pgvector")` / `with_prefix("qdrant")` for multi-store deployments where per-store breakdown matters. Crate placement consistent with iter 271/272: lives in litgraph-retrieval to avoid the circular dep that would arise from putting Retriever wrappers in litgraph-resilience |
| `RecordingRetriever` + `ReplayingRetriever` + `RetrieverCassette` + `retrieve_hash` (iter 274) | Same shape as iter 254 (chat) / 255 (embed) / 256 (tool): `parking_lot::Mutex<Cassette>` shared between recorder and test scope; serde-JSON cassette format with file IO; blake3 over canonical JSON of `(query, k)` for hash key | **Closes the record/replay matrix.** Coverage now spans all four primary axes — the full agent loop (LLM + embedding + tool + retrieval) is CI-deterministic with one consistent VCR-style API. Hash key includes `k` (not just `query`) so different `k` values record/replay independently — important because a `retrieve("X", 5)` and `retrieve("X", 50)` call return semantically different result lists, and conflating their cassette entries would be a debugging trap. `Document` is already `Serialize + Deserialize` so the cassette format is just a JSON serialization of `Vec<{request_hash, query, k, response: Vec<Document>}>`. Same load_from_file/save_to_file convenience, same passthrough-on-miss option for "record-then-fill-gaps" workflows. Lives in litgraph-retrieval (consistent with iter 271-273 retriever-wrapper placement). With this iter the testing-infra matrix completes alongside the metrics-instrumentation matrix from iter 273 — every prod-grade observability and testability pattern now covers the full primary axis set |
| `CircuitBreakerRetriever` (iter 275) | `Arc<dyn Retriever>` + `Arc<CircuitBreaker>` (iter-243 primitive); maps `CircuitCallError::CircuitOpen → Error::Provider("circuit breaker open")` (intentionally NOT matching the 5xx pattern in `is_transient`) | **Brings the breaker to the retriever axis.** Mirrors iter 244 (chat) / 245 (embed). Real prod use: vector store goes down → N consecutive failures trip the breaker → subsequent retrievals fail fast for `cooldown` instead of hammering the sick service. Standard composition pattern is `EnsembleRetriever([primary_with_breaker, backup_bm25])` so circuit-open routes immediately to a degraded but available retrieval. Critical detail: the error message "circuit breaker open" intentionally doesn't match the 5xx-pattern that `is_transient` recognizes, so a `RetryingRetriever` outer doesn't re-trigger the open breaker — exactly the right behavior, since hammering an open breaker just delays recovery. Stack order for the full prod chain (outer-to-inner): `Retrying → Breaker → Timeout → vector store`. Half-open probe semantics are inherited from the iter-243 primitive: exactly one probe call allowed at a time, success closes the breaker, failure resets the cooldown. With this iter the circuit-breaker matrix covers chat (244) / embed (245) / retrieve (this iter) — the three primary axes where it applies (tool axis has a distinct resilience model via iter-159 `TimeoutTool` / `RetryTool`) |
| `BulkheadRetriever` (iter 276) | `Arc<dyn Retriever>` + `Arc<Bulkhead>` (iter-248 primitive) + `BulkheadMode::{Reject, WaitUpTo(Duration)}` (mirrors iter-249's chat/embed mode enum) | **Closes the bulkhead matrix to chat/embed/retrieve.** Same crucial design choice as iter-249: bulkhead-full surfaces as `Error::RateLimited { retry_after_ms: None }`, NOT a custom variant. Reason: `is_transient` already matches `RateLimited`, so `RetryingRetriever(BulkheadRetriever(inner))` retries on rejection (slot may have opened) and `EnsembleRetriever([primary_with_bulkhead, backup])` switches to backup on rejection. No extra wiring needed. Real prod use: pgvector connection pool has 20 slots — cap retrievers at 18 to leave headroom for writes; cap qdrant queries to bound CPU usage; protect in-process HNSW from memory pressure under heavy fan-out. One `Arc<Bulkhead>` can span multiple `BulkheadRetriever` instances when they share an underlying resource (e.g., two retrievers both hitting the same pgvector instance share the connection pool budget). Crate placement consistent with the rest of the iter-271-onward retriever-wrapper line: lives in litgraph-retrieval. With this iter the bulkhead matrix covers chat (249) / embed (249) / retrieve (this iter) — the three primary axes where it applies (tool axis has a distinct resilience model via iter-159) |
| `HedgedRetriever` (iter 277) | Two `Arc<dyn Retriever>` (primary, backup) + `Duration` hedge delay; delegates to `litgraph_core::hedged_call` (iter-250) | **Closes the hedge matrix to chat/embed/retrieve.** Mirrors iter 251 on the retriever axis. Distinct from iter-193 `RaceRetriever`: that issues to all inner retrievers simultaneously (always doubles cost). Hedge only fires the backup when primary exceeds `hedge_delay` — fast-path requests pay zero overhead. Standard pattern from Dean & Barroso 2013 "The Tail At Scale". Real prod use: HNSW with cold pages (1ms p50 / 50ms p99) hedged at 5ms against a replica; multi-region setups where the close-region primary fails over to the far-region backup only on slow calls. Loser future is dropped on tokio cancellation, releasing whatever HTTP/DB resource it held. With this iter the hedge matrix covers chat (251) / embed (251) / retrieve (this iter). Combined with the rest of the retriever-axis resilience suite (Timeout 271, Retrying 272, Metrics 273, Record/Replay 274, CircuitBreaker 275, Bulkhead 276), the retriever has full prod-grade-tooling parity with the chat/embed axes |
| `SingleflightRetriever` (iter 278) | `Arc<dyn Retriever>` + `Arc<Singleflight<String, Arc<Result<Vec<Document>, String>>>>`; hash key reuses iter-274's `retrieve_hash(query, k)` so the cassette format and the coalescing key are deliberately the same shape | **Fourth axis of request coalescing.** Family covers embed (iter 253) / tool (iter 263) / retrieve (this iter); chat axis intentionally NOT coalesced because collapsing model invocations changes user-visible semantics (stochastic outputs would silently merge). N concurrent identical `retrieve(query, k)` calls share ONE inner call; followers get the leader's `Vec<Document>` via `tokio::sync::broadcast`. Different `k` values coalesce independently — `retrieve("X", 5)` and `retrieve("X", 50)` are distinct cache keys, matching the cassette key in iter 274. Errors lossy-stringify to `Error::Provider(s)` (same trade-off as iter 253 / iter 263) since `litgraph_core::Error` isn't Clone. Documented "when NOT to coalesce" guidance: don't wrap retrievers with telemetry side effects that need per-call recording, or with per-caller authorization checks that vary the result. With this iter the coalescing matrix covers all axes where it's safe (3/4 — chat axis intentionally skipped). Combined with the full retriever resilience suite (Timeout 271 / Retrying 272 / Metrics 273 / Record-Replay 274 / CircuitBreaker 275 / Bulkhead 276 / Hedged 277 / Singleflight 278), the retriever has 8 production-grade wrapper patterns matching or exceeding what the chat/embed axes provide |
| `CurrentTimeTool` (iter 279) | Pure-Rust via `chrono` workspace dep (no `chrono-tz` to keep dep weight low); `FixedOffset::east_opt` clamps `tz_offset_hours` to `±26h`; ISO-8601 / Unix / weekday / date / time / tz returned in one JSON object | **Pivot to a fresh tool — date-aware reasoning.** Surprisingly missing from the existing 21+ tool set, despite being one of the most commonly-needed grounding facts for agent reasoning. Without this, LLMs have no reliable way to ground "today" / "next Tuesday" / "two weeks from now" reasoning — their training cutoff is in the past, and any agent prompt that doesn't include a current-date stub will hallucinate. Optional `tz_offset_hours` arg supports fractional offsets (5.5 for IST, -7 for PDT, 0 for UTC default). IANA-name resolution intentionally not supported — that requires the heavy `chrono-tz` dep + an embedded TZ database; the explicit-offset surface is sufficient for most prod cases and keeps the tool light. Tools-utils crate now ships 22+ built-ins; this one is the new "should be in every default ReactAgent toolset" |
| `RegexExtractTool` (iter 280) | `regex::Regex` per-call compile (~µs cost; the regex crate has no public API to cache patterns across calls without a registry); three modes implemented as a small match arm over `find_iter` / `find` / `captures_iter` | **Universal data-extraction primitive — pairs with `WebFetchTool` / `HttpRequestTool` to close the "fetch → extract" loop**. Three modes: `all` returns every match as a string array (the typical case); `first` returns just the first match or null; `captures` returns each match alongside its numbered capture groups so agents can pull semi-structured data (`(\d{4})-(\d{2})-(\d{2})` over a date string returns `["2024-09-15", "2024", "09", "15"]`). Errors on bad regex syntax — surfaces as `Error::InvalidInput` so the LLM can retry with corrected pattern. The `captures` mode handles unmatched optional groups (`(?:...)?` no-match) by emitting JSON null in the corresponding slot, so the array shape is stable across matches. Uses the `regex` workspace dep already in tools-utils — no new third-party. With this iter the data-extraction trio is complete: `WebFetchTool` (fetch HTML), `RegexExtractTool` (extract structured fields), `JsonExtractTool` (deferred — agents can also use `parse_partial_json` from iter 122 in code) |
| `JsonExtractTool` (iter 281) | Hand-rolled JSONPath-lite parser (no third-party dep): `$` root + `.field` access + `[N]` index (negative supported) + `[*]` wildcard. Walks a `Vec<Segment>` over `serde_json::Value` recursively. ~80 LOC parser + walker | **Sibling to iter-280 RegexExtractTool — closes the data-extraction trio.** RegexExtract handles unstructured text; JsonExtract handles JSON. The path syntax is intentionally a tiny subset of full JSONPath: filter expressions (`?(@.age > 30)`), recursive descent (`..`), and slices (`[1:5]`) are NOT supported. Reason: the more complex the syntax, the more LLM confusion (and tool failures). The 4 supported operations cover the typical "drill into API response" use case. The `json` arg accepts BOTH a JSON value AND a string (auto-parsed if string) — handles the case where the LLM passes the JSON inline as a structured object OR forwarded the raw string from a previous tool's output. The `[*]` wildcard implementation deliberately skips elements where the inner path resolves to `null`, so `$.users[*].email` over a fixture where one user has no `email` field returns just the existing emails (rather than `["alice@…", null, "bob@…"]`) — matches the LLM's expected mental model of "give me the emails." Real prod use: `HttpRequestTool` returns a JSON API response; `JsonExtractTool` pulls just the field the agent needs without an intermediate parsing step. With this iter the agent-side data-extraction story is complete: `WebFetch` (HTML), `RegexExtract` (text), `JsonExtract` (JSON) |
| `UrlParseTool` (iter 282) | `url::Url` parse + a custom query-param grouping (`HashMap<String, Vec<String>>` → JSON object where single-value keys stay strings and repeated keys collapse to arrays) | **Pairs with the data-extraction trio — gives agents an "input parsing" toolkit alongside the data extraction**. Real prod use: agent receives an OAuth callback URL `https://app/callback?code=ABC&state=xyz` and pulls `query_params.code` directly; agent validates a user-supplied URL's `host` is on an allowlist before redirecting; agent rebuilds a URL with modified path/query. The repeated-key collapse design (`?tag=a&tag=b&tag=c` → `{tag: ["a","b","c"]}`) matches what the LLM expects when generating tool calls — repeated keys in URL query strings are array-valued. Default ports (443 for https, 80 for http) intentionally return `null` so the agent can distinguish "URL didn't specify a port" from "explicit port 443"; if the agent needs the implicit default, it knows the scheme. Auth components (username/password) extracted into separate fields rather than left embedded in the host. Uses the `url` crate (already a transitive dep via reqwest, just promoted to explicit). With this iter tools-utils ships 25+ built-ins covering: math, HTTP, filesystem, shell, SQL, search engines, web fetch, code exec, notifications, email, audio/image, planning, time, regex, JSON path, URL parse |
| `HashTool` (iter 283) | `blake3::hash` + `sha2::Sha256/Sha512` + `md-5::Md5`; output via `hex::encode`. Algorithm dispatched in a `match` arm with `Error::InvalidInput` for unknown names | **Content fingerprinting on agent demand — fills a gap that previously needed user-side custom tools.** Common prod uses agents face: dedup detection ("have I seen this exact response before?"), content-integrity checks (compare a fetched payload against a known hash), agent-generated cache keys (when the agent needs to derive a stable key from a long string for downstream lookup). Default `blake3` because that's what the rest of litGraph uses internally (hash keys for cassettes, content hashes for dedup, etc) — the agent's hash output and the framework's internal hash output are the same family. Algorithm-choice guidance baked into the rustdoc tells the LLM when to override the default: sha256/sha512 for interop with non-Rust services (most APIs, JWT, file checksums); md5 only for legacy non-security uses (cache keys against systems that already use md5). Tested against known fixed-input vectors (`blake3("hello")`, `sha256("hello")`, etc) for every algorithm — bit-identical regression check. Reuses sha2/md-5/hex crates that were already transitive deps via the reqwest/openssl stack — no new third-party brought in. With this iter tools-utils ships 26+ built-ins; the agent's "small utilities" set covers time, regex, JSON path, URL parse, and hash digest — common pain points for agent workflows that used to require user-side custom tools |
| `Base64Tool` (iter 284) | `base64::engine::general_purpose::{STANDARD, URL_SAFE_NO_PAD}` from the `base64` workspace dep; encode → bytes-to-string, decode → bytes-to-utf8 with `Error::InvalidInput` on non-UTF-8 | **Pairs with `HashTool` to round out the "wire-format" tool set.** Two variants intentionally exposed (vs. a one-size-fits-all alphabet): `standard` is RFC 4648 §4 (`+/=`) for general API payloads; `url_safe` is RFC 4648 §5 (`-_`, no padding) for JWT headers and URL params where `+/=` would need percent-escaping. The `url_safe` choice of NO PAD matches what JWTs actually emit — the standard padded variant is incompatible with the JWT spec, so picking `URL_SAFE_NO_PAD` keeps the tool useful out-of-the-box for `decode jwt[0]` / `decode jwt[1]` workflows. Decoded output is UTF-8 only; binary decode (e.g. raw image bytes) is intentionally not supported because the JSON tool-call format can't return raw bytes anyway — agents that need binary should decode in code via the `base64` crate directly. JWT-header-decode workflow verified against the standard `{"alg":"HS256","typ":"JWT"}` test vector. With this iter tools-utils ships 27+ built-ins; the agent's "wire-format" pair (Hash + Base64) handles content fingerprinting AND format conversion — common needs for agents that interact with APIs |
| `UuidTool` (iter 285) | `uuid::Uuid::{new_v4, now_v7}` + format dispatch over `as_hyphenated` / `as_simple` / `as_urn` | **Default-version choice matters.** `v7` is the default because timestamp-ordered IDs preserve B-tree page locality for DB primary keys — at scale, v4's randomness causes write hotspots that destroy index throughput, while v7's 48-bit-millis prefix keeps inserts mostly-sequential. The right default for the typical agent workflow that mints IDs for downstream storage. `v4` (122 random bits) explicitly available for opaque tokens where ID ordering would leak information (session tokens, request IDs visible to users). `count` capped at 100 — agents that loop generating IDs typically want a small batch; the cap is a sanity guard against an LLM stuck in a generation loop. Three format variants exposed: `hyphenated` (default, the canonical 36-char form), `simple` (no-hyphen 32-char hex for systems that strip dashes), `urn` (`urn:uuid:...` for IRIs / RDF). Always returns an array (even for `count=1`) so caller code doesn't special-case length. Sortable-by-creation-time property of v7 verified by a test that generates 10 IDs serially with sleeps and checks that string-sort matches insertion order. With this iter tools-utils ships 28+ built-ins; the agent's "small primitives" set (time / regex / JSON path / URL / hash / base64 / UUID) covers the most common general-purpose pain points |
| `TextDiffTool` (iter 286) | `similar::TextDiff::from_lines` for the LCS / Myers diff algorithm; unified-format renderer via `unified_diff().context_radius(n).header(...)`; structured format walks `iter_all_changes()` and bucketize by `ChangeTag` | **Code-change / document-review / audit workflows now have a built-in.** Real prod scenarios: code-change agents that need to summarize "what did the refactor change?" before applying or reporting; document-review agents reading two versions of a config / spec / policy; test-failure analysis diffing actual vs expected output. Two output formats reflect two different consumption patterns: `unified` (default, +/-/@@ traditional diff) is compact and easy for the LLM to summarize back to the user; `structured` (JSON with separate `additions` / `deletions` arrays and a `summary: {added, removed}` count) is better when the agent needs to programmatically filter or count changes. `context_lines` (default 3) controls the unchanged-line padding around each hunk in unified mode — `0` produces minimal hunks (just the changes), higher values give more readable diffs. Required adding the `similar` crate as a dep (small, focused, no transitive bloat — already the canonical Rust diff lib). Tools-utils now ships 29+ built-ins |
| `SentenceSplitter` (iter 287) | Char-by-char terminator scan (`.`, `!`, `?`) + look-ahead requiring whitespace + uppercase / digit / opening-quote start; abbreviation lookup via lazy-static `HashSet` of ~70 common entries; ellipsis collapse via run-length consume of consecutive same-terminator chars | **Closes the 🟡 sentence-splitter gap that was previously deferred to recursive-char.** Real prod use: sentence-level RAG (retrieve the most relevant sentence, not a 1000-char chunk), fine-grained QA citations (answer cites a specific sentence by index), translation pipelines (translate sentence-by-sentence to keep semantic units intact), summarization (reduce a doc to its top-N sentences). Rule-based; no NLP model dependency, no Python interop, no inference latency. Coverage: ~95% of clean prose. Handles all the common false-positive traps that defeat naive `text.split(".")`: titles (Dr., Mr., Prof.), Latin abbreviations (e.g., i.e., etc.), business suffixes (Inc., Ltd., Co.), geography acronyms (U.S., U.S.A., U.K.), decimal numbers (3.14), ellipses, trailing close-quote/paren attached to the prior sentence. The abbreviation table is curated, not exhaustive — for specialized corpora (clinical notes, legal docs with their own abbreviation conventions), users should layer a domain-specific list or fall back to a Punkt-style ML splitter. `with_min_length(n)` glues fragments shorter than `n` chars onto the prior sentence so callers don't get `.` or `1.` as standalone outputs. Splitters crate now ships 10 |
| `JsonLinesSplitter` (iter 288) | Per-line `serde_json::from_str` validate + optional `to_string_pretty` re-serialize; preserves the original line-as-emitted by default (no whitespace munging) | **NDJSON / JSONL aware splitter — distinct from `JsonSplitter`.** `JsonSplitter` parses one big JSON document and navigates its tree; `JsonLinesSplitter` treats input as line-delimited JSON and emits one chunk per record. Real prod use: structured-log streams (Datadog/Loki/ELK ship NDJSON), OpenAI fine-tune format (`{"messages":[...]}` per line), HuggingFace streaming datasets, replay traces persisted as NDJSON. `with_pretty(true)` re-formats each record as multi-line JSON for readability when downstream embedders prefer the expanded form; `with_skip_invalid(true)` silently drops malformed lines (default keeps them verbatim in the output so the caller can see and report them). Skips blank lines. Handles CRLF endings cleanly via `text.lines()`. Round-tripped against an OpenAI fine-tune fixture (`{"messages":[{"role":"user", ...}]}` per line). Splitters crate now ships 11 |
| `detect_drift` + `DriftReport` + `CaseDrift` (iter 289) | Index baseline cases by `input` string; per-(case, scorer) tuple compare with threshold; bucketize into regressions / improvements / stable_failures + diff the case sets for missing/new; aggregate-mean deltas per scorer | **Pivot to eval — per-case drift detection between two eval reports.** Existing eval primitives (`run_eval`, `run_eval_concurrent`, scorers, LLM judge, synthetic gen) produce `EvalReport`s but didn't have a drift comparator. Aggregate score changes ("win rate went from 87% to 88%") hide the case-level story; this primitive surfaces what actually changed when a model upgrade or prompt tweak ships. The four buckets each map to a different action: **Regressions** (passed → failed) are the most actionable — fix or roll back; **Improvements** (failed → passed) confirm the change is helping; **Stable failures** (still failing in both) are likely dataset issues, not model issues; **Aggregate deltas** are the headline summary for changelogs. Threshold-tunable (default 0.5 for binary scorers; tune to ~0.1 for continuous scorers like embedding cosine). `DriftReport::has_regressions()` is the natural CI gate — `if drift.has_regressions() { fail_build() }`. Lives in `litgraph-core` alongside `eval_harness` so drift detection sits next to the report production. Pairs with `EvalReport`'s serde-derived JSON serialization to enable a "save baseline → CI loads + diffs" workflow without any extra plumbing |
| `mcnemar_test` + `McNemarResult` (iter 290) | Per-scorer 2×2 contingency cell counts `[a, b, c, d]` walked from paired EvalReports; continuity-corrected `(|b-c|-1)²/(b+c)` chi-squared statistic; p-value via χ²(1) ↔ Z² identity + Abramowitz & Stegun 7.1.26 erf polynomial approximation (no extra dep) | **Pairs with iter-289 drift detector — answers "is the change real?"** Drift surfaces the cases that moved; without significance testing, callers don't know whether 5 regressions out of 100 cases is meaningful or noise. McNemar's test (Quinn McNemar 1947) is the textbook tool for paired binary outcomes — perfect fit for "case passed in baseline, did it pass in current?". Per-scorer results so multi-scorer evals (exact_match + jaccard + llm_judge) get separate significance flags. Output includes the raw contingency cells (`a`/`b`/`c`/`d` counts) so users can verify the math; `chi_squared` for the test statistic; `p_value` for the two-tailed approximation; `significant_at_05` boolean for direct CI gating; `small_sample` flag (`b+c < 25`) since the chi-squared approximation is unreliable below that — flagged-not-suppressed so callers know to interpret carefully or fall back to an exact binomial test. Pass/fail threshold pinned at `score >= 0.5` to match iter-289 conventions; for continuous scorers where binary thresholding loses information, callers should layer a paired t-test or Wilcoxon signed-rank on the raw scores. Self-contained — no `statrs` dep added; the erf approximation gives 7-sig-fig accuracy which is plenty for sub-0.05 / sub-0.01 boundary checks |
| `SemanticCachedRetriever` (iter 291) | `Arc<dyn Retriever>` + `Arc<dyn Embeddings>` + `parking_lot::Mutex<Vec<CacheEntry>>` (linear scan); cosine similarity per entry; LRU eviction by removing the head; optional TTL via per-entry `Instant` | **Genuinely fresh wrapper distinct from iter-278 SingleflightRetriever**: that coalesces *concurrent* identical calls (same query+k arriving at the same time share one inner call); this caches *across* calls when queries are semantically similar. Real prod use: FAQ-style agents where users phrase the same question differently — "How do I reset my password?" / "I forgot my password, help" / "password recovery steps" all hit the same cache line because their embeddings are close. Knobs: `with_threshold(t)` (default 0.95) — tune lower for chattier-FAQ matching, higher for strict near-paraphrase; `with_max_entries(n)` LRU cap (default 1000); `with_ttl(d)` for cache expiry (useful when the underlying corpus updates). Different `k` values keep separate cache lines (result-list shapes differ). Linear scan over entries — for thousands of entries that's thousands of cosine-similarity computations per call; tune `max_entries` to latency budget. Win condition: when retrieval cost > embedding cost (true for hybrid, reranked, or large-corpus retrievers) AND queries cluster semantically (true for FAQ / support / lookup agents). Cosine similarity rolled inline (no `nalgebra` dep) since the math is 6 lines |
| `CachedEmbeddings` (iter 292) | `Arc<dyn Embeddings>` + two separate `parking_lot::Mutex<Vec<(Key, Entry)>>` LRU caches (one for `embed_query` keyed by `String`, one for `embed_documents` keyed by `Vec<String>`); per-entry `Instant` for optional TTL | **Pairs with iter-253 SingleflightEmbeddings — same axis, different time window.** SingleflightEmbeddings coalesces *concurrent* identical calls (in-flight window). CachedEmbeddings caches *across* calls (until TTL or LRU eviction). Real prod stack: outer `CachedEmbeddings` + inner `SingleflightEmbeddings` + real provider — cache hits skip even the dedup; cache misses go through dedup so concurrent misses still coalesce. Different cache for `embed_query` vs `embed_documents` because the response shape differs (Vec<f32> vs Vec<Vec<f32>>); they share the configurable `max_entries` cap. `embed_documents` keyed by `Vec<String>` exactly — different orderings are different keys (by design, since embedding output IS order-sensitive). LRU eviction via `cache.remove(0)` when over cap. Default config (no TTL, 1000 entries) is the sensible default when the embedding model is fixed; turn on TTL when the corpus updates and stale embeddings would mislead retrievers. Win condition: agents over a fixed query corpus that gets re-evaluated, popular search terms, eval-harness runs that re-embed the same dataset — anywhere queries repeat across calls |
| `HtmlSectionSplitter` (iter 293) | Walk lower-cased HTML tracking depth per configured tag (open/close pairs); regex-based pre-strip of `<script>`/`<style>`/`<!-- -->`; non-content tags between sections cleaned to plain text via a generic `<[^>]+>` strip + whitespace collapse | **Sibling to iter-25-era HtmlHeaderSplitter — different splitting axis.** HtmlHeaderSplitter uses `<h1>`–`<h6>` hierarchy; this uses HTML5 semantic-block tags. Many modern docs sites and blog templates use `<article>` / `<section>` containers without explicit heading-based sectioning; for those, the heading-splitter produces zero useful chunks but this splitter produces one per content container. Default tag set covers the HTML5 sectioning + landmark roles: `article`, `section`, `main`, `aside`, `nav`, `header`, `footer`. `with_tags([...])` for site-specific customization (e.g. `with_tags(["div.blog-post"])` to target a specific class — though the impl matches by tag name only, not selector). Nested same-tag pairs deliberately collapse to the outer match: a `<section><section>...</section></section>` emits as one chunk, not two. To split nested sections, callers can run the splitter recursively on each chunk. `with_drop_outside(true)` for strict-section-only output (text outside any matched tag is dropped); default `false` emits outside content as additional chunks so nothing is lost. Splitters crate now ships 12 |
| `StackExchangeLoader` (iter 294) | `api.stackexchange.com/2.3/` REST + the `withbody` filter to include rendered HTML in question/answer responses; tagged-OR semantics via `tagged=tag1;tag2` semicolon-joined query param; Rayon-parallel batch fetch of accepted answers (chunks of 100 IDs per request — the API's own batch cap); `gzip` feature was attempted but reqwest's gzip isn't always compiled — removed for portability | **Pivot to a fresh loader covering community Q&A.** 100+ Stack Exchange sites all reachable via the same API: stackoverflow, serverfault, superuser, unix, dba, math, physics, etc. Real prod use: technical agents grounded on community Q&A as a complement to authoritative docs (LangChain alone often misses real-world workarounds, e.g., "tokio async drop pattern" is in SO answers but not in tokio docs); trend monitoring (pull tag-`rust` daily to spot user pain points); doc supplementation. Output design: question title + body concatenated into the Document content; optional accepted-answer body appended under a `--- Accepted Answer ---` divider. The "highest-voted answer" pick is implicit via the API's `sort=votes` order — first answer per question wins. API key is optional but recommended (`with_key(...)`) for ~10k req/day quota vs ~300 unkeyed. Default `max_questions = 30` covers the typical "give me the top recent items" use case; bump for batch-corpus building. Offline-testable via `pub fn question_to_document(&self, &Question, Option<&str>)` that lets unit tests validate the conversion without HTTP. Loaders count: 29+ |
| `coerce_one` + `coerce_stream` (iter 318) | Schema-type detection via attribute presence (no isinstance imports of pydantic — `_looks_like_pydantic` checks `hasattr(cls, "model_validate") or hasattr(cls, "parse_obj")` for v2/v1 detection without importing pydantic; `_looks_like_dataclass` uses stdlib `dataclasses.is_dataclass`; `_looks_like_typeddict` checks `__total__` + `__annotations__`); per-type dispatch in `coerce_one` falls through to generic `cls(**chunk)` keyword construction with a clear error message on TypeError; `coerce_stream` is a thin async-generator wrapper that delegates each chunk through `coerce_one` and lets exceptions propagate immediately | **Closes the IDE-narrow-types gap that the iter-317 functional API exposed.** With `Workflow.astream` yielding dict chunks, callers reading `chunk["step"]` get no IDE autocomplete and no static type-checking — Python streams stay weakly typed where the Rust side already has `#[derive(GraphState)]` for compile-time-typed state. `coerce_stream(stream, MyState)` is the bridge. **Why three schema kinds**: pydantic for full validation + type coercion (most common in production agents); dataclass for stdlib-only environments where pydantic is too heavy a dep; TypedDict for users who want types without runtime instantiation cost. The dispatcher detects each via attribute presence rather than isinstance — this avoids importing pydantic when callers don't use it (a 5-10 MB+ module load tax skipped by default). **Pydantic v1 ↔ v2 fallback**: v2 ships `model_validate`; v1 ships `parse_obj`. The dispatcher tries v2 first, falls back to v1. Both versions are still in production use — pydantic v2 broke API compat with v1, and many large codebases haven't migrated. Supporting both means litgraph users on either version get coercion without rework. **Non-dict pass-through is essential**: streams emit a mix of typed-dict events AND non-dict sentinels (progress percentages, cancellation tokens, raw values). `coerce_one` returns non-dict chunks unchanged so users can `coerce_stream(s, MyState)` without filtering sentinels first. Pinned by `test_coerce_stream_passes_through_non_dict_chunks` with a 4-element source mixing dicts, strings, and ints. **Error propagation is fail-fast**: malformed chunks stop the stream immediately so callers see the problem at the first failure point rather than silently swallowing data. Pinned by `test_coerce_stream_propagates_errors_immediately` with a 3-element source where chunk 2 is invalid — collector gets to chunk 1, then raises on chunk 2, never reaches chunk 3. **Pydantic type coercion** is the marquee win over dataclass: `coerce_one({"value": "42"}, PydanticState)` returns `value=42` (int) — the str→int coercion is what production agents need when streams cross JSON boundaries (HTTP/network) where everything serializes as strings. Pinned by `test_coerce_one_pydantic_validation_coerces_types` (skipped on pydantic-less envs, exercises the path on installed envs). 11 tests cover: dataclass success, dataclass extra-field error, dataclass missing-field error, TypedDict passthrough, Pydantic v2 success (skipped if absent), Pydantic type coercion, non-dict passthrough (3 sentinel cases), 3-chunk dataclass stream, mixed-dict-and-sentinel stream passthrough, stream error propagation, integration with `@entrypoint`'s `.astream()`. Stubs at `litgraph-stubs/litgraph-stubs/coerce.pyi` for IDE autocomplete |
| `@entrypoint` + `@task` + `Workflow` (iter 317) | maturin **mixed layout**: added `python-source = "python"` to `[tool.maturin]` in pyproject.toml + `module-name = "litgraph.litgraph"` so the native module ships as `litgraph.litgraph` and the pure-Python sugar at `python/litgraph/__init__.py` re-exports it. The `__init__.py` wraps native import in `try/except ImportError` so the package is loadable in dev environments without a built wheel — the pure-Python sugar (`functional.py`) works without the native module, useful when iterating on Python code; production installs always have the native module. `@task` is a tagging decorator (sets `fn.__litgraph_task__ = True` for future tooling introspection); function is unchanged at runtime. `@entrypoint(checkpointer=...)` returns `Workflow` instance with `.invoke()` (sync convenience via `asyncio.run`), `.ainvoke()` (await directly), `.astream()` (async iterator — v1 yields single `{"final": <result>}` chunk; future iters can yield per-task events when StateGraph runtime integration ships). `Workflow.__call__` returns the underlying coroutine so `await workflow(...)` works without `.ainvoke()`. `__slots__` carefully omits `__doc__` because slot-defined `__doc__` would shadow class docstring (the bug surfaced during initial test run as `ValueError: '__doc__' in __slots__ conflicts with class variable` — fixed) | **Closes the LangGraph functional-API parity gap** that's been open in the Python ergonomics list. LangGraph users transitioning to litgraph can keep their existing code shape (`@task` async fns, `@entrypoint` workflow root) without rewriting to the StateGraph DSL. **Why pure-Python decorators rather than PyO3-bound**: `@task` is a tagging decorator (no runtime work); `@entrypoint` wraps an async fn in a Workflow class. Both are pure-Python concepts with no native-side benefit — implementing in PyO3 would just add binding boilerplate. The mixed-layout is the right call. **Future StateGraph integration**: v1 of the functional API delegates to plain async-await execution. Each `await task_fn(...)` runs the function directly — no graph node, no checkpoint. Future iters can intercept these awaits (via the `__litgraph_task__` marker) and route them through StateGraph runtime so checkpointing per-task becomes automatic. The marker decorator is the foundation that makes that future work possible without breaking the v1 API. **Defensive errors**: sync function passed to `@entrypoint` → `TypeError` at decoration time with a guidance message ("Wrap with `async def` or use StateGraph DSL directly"). `.invoke()` called from inside a running event loop → `RuntimeError` redirecting to `.ainvoke()` instead of letting `asyncio.run()` crash deep in the workflow with the unhelpful "asyncio.run() cannot be called from a running event loop" message. Both pinned by tests. **Stubs**: `litgraph-stubs/litgraph-stubs/functional.pyi` ships type stubs so IDE autocomplete works on the new decorators. **Native-module tolerance**: importing `litgraph` without a built native module logs an `ImportWarning` (not an error) so the pure-Python sugar still loads. The `if __all__ extends` pattern keeps the export list correct in both built + unbuilt modes. 13 Python tests cover: `@task` adds marker, decorated fn still callable, `@entrypoint()` returns Workflow, `.invoke()` runs the fn, `.ainvoke()` returns coroutine, task chaining inside workflow body (composes 2 tasks `(5+1)*2 = 12`), sync function → TypeError, checkpointer arg stored, `.invoke()` inside running loop → RuntimeError, `repr` includes name + Workflow tag, direct-call returns coroutine, `.astream()` yields `{"final": value}` chunk, name + docstring preserved via `functools.update_wrapper` |
| `kendall_tau` + `ScorerCorrelation.kendall` field (iter 316) | Naive O(n²) double loop counting concordant/discordant/tie-x-only/tie-y-only pairs via signum comparison `dx.signum() == dy.signum()`; tau-b denominator `sqrt((n_c+n_d+n_x)·(n_c+n_d+n_y))` with `Option<f64>` zero-denominator guard; `ScorerCorrelation` struct extended with `kendall: Option<f64>` field populated alongside pearson + spearman in `correlate_scorers` | **Closes the rank-correlation gap left by iter-308 Pearson + Spearman.** Three correlation coefficients now ship side-by-side in `ScorerCorrelation`. The why-three: Pearson is parametric (assumes linear + Gaussian residuals; sensitive to outliers); Spearman is non-parametric over ranks but uses Pearson formula on those ranks (still noise-sensitive when many ties exist because the average-rank convention loses information); Kendall's tau-b counts pairwise agreements directly with full tie correction in the denominator — more robust on tied + heavy-tailed + small-sample data. Common practice in eval reporting: show both Spearman AND Kendall — agreement validates the rank-relationship, divergence flags problematic data structure (excessive ties, outliers, non-monotonic relationships) that single-metric reports would miss. The split tie-handling in tau-b: `n_c+n_d+n_x` on one side of the denominator counts pairs that aren't tied in x; `n_c+n_d+n_y` counts pairs that aren't tied in y. Both-ties pairs contribute to NEITHER side (matches the unbiased-estimator convention in Kendall 1948). Pinned by `kendall_handles_ties_in_x_only` test asserting τ-b = 5/√30 exactly for the fixture xs=[1,1,2,3], ys=[10,20,30,40]. **Why naive O(n²) not Knight 1966 O(n log n)**: Knight's algorithm uses a merge-sort-based concordance counter for n log n complexity. For eval-scale n (thousands of cases at most), n² is fine — a 1000-case run is 1M comparisons, sub-millisecond. Ship simple now; optimize when/if a 100k-case eval shows up. **`Option<f64>` triggers**: length mismatch (programmer error), n<2 (Kendall literally needs 2+ pairs), zero denominator (full ties on one side — happens when xs is constant; same condition Pearson hits). The `kendall_returns_none_for_constant` test pins this with xs=[3,3,3,3] (full x-ties → n_c+n_d=0, n_x=6, n_y=0 → denom = sqrt(6·0) = 0 → None). The eval-decision toolkit now ships 8 primitives: detect_drift (iter 289), mcnemar_test (iter 290), wilcoxon_signed_rank_test (iter 297), paired_effect_size (iter 298), bootstrap_eval_ci (iter 299), correlate_scorers w/ Pearson+Spearman+Kendall (iters 308, 316), permutation_test (iter 313). 9 tests cover: perfect positive (all conc → τ=1), perfect negative (all disc → τ=-1), hand-calculated balanced-swap fixture (4 conc + 2 disc → τ=1/3 exact), tied-x-only with exact τ-b = 5/√30 (the tie-correction pin), constant input → None, length mismatch → None, n<2 → None, agreement with Spearman on monotonic data (sanity), correlate_scorers populates the new kendall field |
| `wer` + `cer` + private `token_levenshtein` / `char_levenshtein` (iter 315) | `tokenize_lower` (existing helper) for WER, `to_lowercase().chars().collect::<Vec<_>>()` for CER; both delegate to a Levenshtein implementation specialized over `&[String]` (WER) or `&[char]` (CER) — same rolling-row DP O(min(m,n)) space pattern as the existing char-level `levenshtein` function in this file but specialized over the right element type to avoid string-slicing per comparison; final scaling `dist as f32 / max(1, ref_len) as f32` so empty-ref doesn't divide by zero | **Closes the lower-is-better metric gap** that ROUGE/BLEU/chrF/METEOR/embedding_cosine don't address — those are all higher-is-better. ASR / OCR / MT engineers prefer error-rate metrics ("1 in 10 words wrong" reads more directly than "BLEU 0.78") and many production reporting tools (jiwer, kaldi scoring scripts, sclite) ship WER/CER as the primary number. Now litgraph's eval suite covers both polarities. **WER vs `levenshtein_ratio` distinction**: `levenshtein_ratio` (already in evaluators.rs since iter ~30) is char-level, capped to `[0, 1]` via `1 − dist/max(|a|, |b|)`. WER is word-level (more common ASR convention) and unbounded above 1.0 — a candidate twice as long as the reference with all wrong words has WER ≈ 2.0. Different communities expect different conventions; both ship. **The CER < WER pin on morphological diffs** (`cer_lower_than_wer_for_morphological_diff` test): "running" vs "runs" → WER = 1.0 (1 substitution / 1 ref word) but CER ≈ 0.571 (4 char edits / 7 ref chars). The asymmetry surfaces what's actually similar at the character grain when word-level matching gives up entirely — useful for diagnosing "the ASR is close but always inflects differently" cases that WER alone would mark as total failures. **CER whitespace significance**: missing space counts as 1 edit (`"hello world"` vs `"helloworld"` → CER = 1/11 ≈ 0.0909). This is the standard CER convention — a whitespace-stripped CER would mask real ASR errors that drop word boundaries. Pinned by `cer_treats_whitespace_significantly` test. **WER can exceed 1.0**: pinned by `wer_can_exceed_one` (1-word ref + 5-word disjoint cand → WER > 1.0). The unbounded shape matters because it lets users compare "30% wrong" vs "200% wrong" cases meaningfully — a `[0, 1]`-clamped metric would saturate. **Why specialized Levenshtein impls not generic over T: PartialEq**: monomorphizing twice (once for `&[String]`, once for `&[char]`) avoids any chance of vtable indirection in the inner loop and the implementations are 12 LOC each — tiny code-size cost. Could refactor to a generic helper later if a third Levenshtein-needing primitive shows up; for now KISS. **Lowercase normalization** matches the rest of the evaluator family for consistency. **Public API**: `wer(ref, cand)` and `cer(ref, cand)` returning `f32` directly (no diagnostic struct — the metrics are atomic numbers; if callers want edit-by-edit details, layer a separate `wer_alignment` primitive in a future iter). The eval suite is now 5 NLG-side metrics + 7 decision primitives + 4 error-rate / similarity functions. Multi-metric reports across the higher-better and lower-better axes triangulate failure modes. 14 tests cover: WER identical → 0, single substitution / deletion / insertion → 1/3 (3-word ref), WER exceeds 1.0 (the unbounded pin), empty ref+cand → 0, empty cand → 1, lowercase normalization; CER identical → 0, single-char sub → 1/5, whitespace significance (the convention pin), empty edge case, lowercase, CER < WER on morphological diff (the design pin) |
| `meteor_lite` + `meteor_exact` + `MeteorScore` + `stem_light` (iter 314) | Greedy alignment in two passes: pass 1 exact-match (each cand token grabs first unmatched ref position with `==`); pass 2 stem-light (only for cand tokens still unmatched — applies `stem_light` suffix strip to both sides for comparison). Chunk count via single linear scan over `alignment: Vec<Option<usize>>` tracking `prev_ref` — increments chunks on (a) None alignment slot or (b) ref position not equal to `prev_ref + 1`. F-mean uses Banerjee-Lavie default α=0.9 (recall-weighted). Penalty `0.5 · (chunks / matches)^3` (γ=0.5, β=3 defaults). Stem-light strips `ies/ing/ed/ly/es/s` in priority order with min stem length 3 to avoid over-aggressive stripping (`is`→`is`, not `i`) | **Closes the alignment gap left by ROUGE (iter 304) / BLEU (iter 305) / chrF (iter 307).** Those metrics reward n-gram overlap but don't model alignment beyond the n-gram window — a candidate with the right words in wrong order ("cat the sat" vs "the cat sat") gets full BLEU-1 credit (3/3 unigrams match) and decent ROUGE-1 (3/3) despite the bad ordering. METEOR's chunk-fragmentation term explicitly penalizes that case: same-order candidate has 1 chunk + small penalty; scrambled candidate has 3 chunks + larger penalty. Pinned by `meteor_word_order_matters` test asserting `scrambled.score < same-order.score` while `scrambled.matches == same.matches`. **METEOR-lite not Porter+WordNet**: full METEOR (Banerjee-Lavie 2005) uses three matchers — exact, Porter stemmer, WordNet synonyms/paraphrases. WordNet is a 30 MB English-only lexical database; pulling it would dominate the crate's dep weight and only help English. Suffix-strip stem-light covers ~80% of the stem signal Porter would for English (`walks`/`walk`, `playing`/`play`, `walked`/`walk`) at zero dep cost. For non-English use chrF (iter 307, language-agnostic char n-grams). For paraphrase credit layer an embedding-based scorer (could ship as iter-future BERTScore-lite). For strict-vocabulary scoring use `meteor_exact` (no stem fallback). **The chunk penalty floor**: identical 6-token strings score ≈ 1·(1 − 0.5·(1/6)^3) ≈ 0.9977 not 1.0. The penalty asymptotes to 0 only for infinite match length. Reference Java/Python impls behave the same way. Pinned by `meteor_identical_strings_near_one` test asserting `score > 0.99` and exact penalty arithmetic. Documented in the rustdoc so callers don't expect 1.0 on identical inputs. **Stem priority order matters**: stripping `s` before `es` or `ies` would produce wrong stems (`flies` → `flie` instead of `fli`). The suffix list `["ies", "ing", "ed", "ly", "es", "s"]` is checked longest-first to dodge that — pinned by `stem_light_strips_common_suffixes` test asserting `walks → walk`, `playing → play`, `quickly → quick`. **α=0.9 default is recall-weighted**: matches Banerjee-Lavie's empirically-optimal setting for human-correlation. A cand with high precision but missing reference content scores lower than a cand with full recall but extra fluff (within reason — fluff still hurts via the over-generation chunk count). The recall-weighting matches ROUGE's emphasis but with the alignment penalty layered on. **Diagnostic `MeteorScore` struct** surfaces P/R/F-mean/penalty/matches/chunks/lengths so callers can answer "why did METEOR drop from 0.85 to 0.62?" — usually it's chunks/matches ratio jumping (output got shuffled) or matches dropping (vocabulary diverged). The NLG-eval suite is now 4 metrics: ROUGE (recall-emphasizing word F), BLEU (precision-weighted word + brevity penalty), chrF (char-level morphology-aware), METEOR (alignment + fragmentation-aware). Multi-metric reports triangulate failure modes. 11 tests cover: identical-near-1, empty inputs → 0, disjoint vocab → 0, word-order penalty (scrambled < same-order, the design pin), stem credit (lite ≥ exact), partial match < 1, struct diagnostics, recall-weighted F-mean math verified by exact arithmetic (α=0.9 → F = 0.5/0.55 ≈ 0.909 for P=0.5 R=1.0 fixture), case folding, lite-≥-exact monotonicity (lite never scores below exact), stem_light suffix priority |
| `permutation_test` + `PermutationResult` (iter 313) | Per-scorer paired-diff loop using existing pairing scheme (`baseline_by_input` HashMap); per-scorer Monte Carlo: build observed mean = `sum/n`, then `n_permutations` iterations, each iteration loops over diffs and uses `rng.next_u64() & 1` lowest-bit to decide sign-flip (`if bit == 0 { d } else { -d }`), accumulates sum/n for permuted mean, increments `at_least_as_extreme` counter on `|perm_mean| >= |observed|`; per-scorer seed via `seed.wrapping_add(fnv1a_64(scorer_name))` so adding/removing a scorer doesn't shift the per-scorer permutation sequence; PRNG via `PermXorshift64` — duplicated from iter-299 bootstrap to avoid making bootstrap's PRNG `pub` (4-line maintenance cost vs API contamination); FNV-1a hash inline (4-line constants `0xcbf29ce484222325` / `0x100000001b3`) | **Closes the gap iter-297 Wilcoxon's `small_sample` flag explicitly points users at.** When n < 20, Wilcoxon's normal-approximation-to-rank-sum-distribution gets sketchy — the warning flag is a "use a different test" signal, but a different test wasn't shipped. Now it is. **Why this not Wilcoxon**: Wilcoxon makes a distributional assumption (rank sum approximately Gaussian under H₀) that gets unreliable for small n. Permutation makes NO distributional assumption — under H₀ (no systematic difference between baseline and current), each diff's sign is equally likely to be + or − by symmetry, so sign-flipping random subsets directly Monte-Carlo-samples the null distribution. The resulting p-value is exact for sufficiently large `n_permutations` (Monte Carlo error scales as `1/sqrt(n_permutations)` — 1000 permutations gives sub-0.05 boundary determination, 10000 tightens around 0.01). **Why this not bootstrap CI**: bootstrap (iter 299) gives uncertainty BOUNDS on a metric (mean ± CI); permutation gives a SIGNIFICANCE p-value for "is the observed shift real or could it have arisen by chance?". Both reuse the xorshift PRNG and the same seed-determinism contract, but they answer different questions and both belong in the eval suite. **Per-scorer seed isolation matters for CI**: a naive implementation would derive each permutation's RNG from a single global seed, which means adding scorer "y" to a report would shift the permutation sequence used for scorer "x" → "x"'s p-value drifts even though its data didn't change. The fix: `scorer_seed = seed + fnv1a_64(scorer_name)` so each scorer's per-permutation sequence is determined entirely by its own name. Adding/removing scorers no longer shifts other scorers' p-values across runs — the CI report stays diffable as users iterate on scorers. Pinned by `permutation_per_scorer_seed_isolated` test that runs the same scorer "x" in two reports (one with scorer "y" added) and asserts identical p-values. **The small-sample correctness pin** (`permutation_small_n_works_where_wilcoxon_warns`): n=8 with all +0.1 diffs has theoretical exact p = 2/256 = 0.0078 (only 2 of 2^8 sign-flip permutations have |permuted_mean| >= 0.1: the all-positive case and the all-negated case). Monte Carlo at 10000 samples lands well within the tolerance band (asserted between 0.001 and 0.02). This is the gold standard for the small-sample regime where Wilcoxon would just shrug. **Reuses iter-299's xorshift64 contract**: same seed-zero substitution (`0x9E3779B97F4A7C15` golden-ratio constant), same `next_u64` core (`x ^= x<<13; x ^= x>>7; x ^= x<<17`), same statistical-quality guarantees. Duplicated rather than re-exported to avoid contaminating the bootstrap module's API surface — the 4-line struct + impl is cheaper to maintain in two places than to refactor for sharing. **Edge cases handled**: empty pair set → p=1, n_permutations=0 clamps to 1 (still produces a valid result), all-zero diffs → p=1 (every permutation has the same mean as observed, so every permutation is "at least as extreme"). The eval-decision toolkit now ships 7 primitives: detect_drift (iter 289), mcnemar_test (iter 290), wilcoxon_signed_rank_test (iter 297), paired_effect_size (iter 298), bootstrap_eval_ci (iter 299), correlate_scorers (iter 308), permutation_test (this iter). Multi-metric CI gates can stack arbitrarily across these. 9 tests cover: no-diffs → p=1.0 (every permutation has 0 mean = observed 0), uniform improvement of 25 cases → p < 0.01, uniform regression mirror, balanced 10/10 → not significant, small-sample n=8 with theoretical p ≈ 0.008 (the design pin), reproducibility (same seed → same p), n_permutations=0 clamps to 1, per-scorer alpha-sort, per-scorer seed isolation (the CI-stability pin) |
| `JsonPatchTool` + `json_patch` free fn (iter 312) | RFC 6901 pointer parsing via `path[1..].split('/').map(decode_token)` where `decode_token` does `replace("~1", "/").replace("~0", "~")` in spec-mandated order so `~01` → `~1` not `/`; per-op dispatch in `apply_op` over `op` field; mutable traversal via `pointer_get_mut_path` walking `Vec<String>` tokens through nested `Value::Object`/`Value::Array` matches; atomicity via `for op in patch { apply_op(&mut working, op).map_err(...)?; }` — any failure short-circuits and the working copy is dropped; the Tool trait does an upfront `.clone()` of the input document so the original is preserved unaffected | **Closes the "agent mutates structured doc without re-emitting whole thing" gap.** Real prod scenarios: agent updating a YAML/JSON config file (e.g. `[{op: "replace", path: "/database/host", value: "new"}]`); agent appending a workflow step (`[{op: "add", path: "/steps/-", value: {...}}]`); workflow state machine using `test` as a CAS precondition (`[{op: "test", path: "/status", value: "pending"}, {op: "replace", ...}]` only updates if status was pending); collaborative-editing audit logs where each change is a discrete patch op; API mocking where mutations are described as patches against a base doc. **Pairs with iter-281 JsonExtractTool**: that reads JSON via JSONPath-lite (`$.users[0].email`); this writes via JSON Pointer (`/users/0/email`). Different syntaxes match the underlying RFCs — JSONPath is the de-facto read syntax (jq, jsonpath-plus, etc); JSON Pointer (RFC 6901) is what RFC 6902 Patch mandates for write paths. Both ship because mixing them lets agents read-then-modify with the right tool for each phase. **The atomicity guarantee is real**: ops apply sequentially over a working copy. Any failure (missing path for `replace`/`remove`/`test`, type mismatch, `test` assertion fail, invalid array index) short-circuits the patch loop and the function returns Err — the partial working copy is dropped. The Tool trait's wrapper clones the input document so the original is preserved. Pinned by `test_fail_aborts_whole_patch` test (test op fails before a subsequent add — the add must NOT have applied — verified by Err result). The atomicity is a hard contract for prod use: workflows that batch multiple writes via patch can roll back cleanly without state leaks. **Spec-mandated escape decode order**: `~1` (slash) MUST be decoded before `~0` (tilde), otherwise `~01` decodes to `/1` instead of the intended `~1`. The `decode_token` function calls `.replace("~1", "/")` first, then `.replace("~0", "~")`. Pinned by the `parse_pointer_escapes` test with the `~01 → ~1` fixture. **`replace` vs `add` semantics distinction**: both write to a path, but `replace` REQUIRES the path to exist (errors otherwise) while `add` creates if missing. This matches RFC 6902 — `replace` is "overwrite an existing thing", `add` is "put a thing there, regardless". Confusing them silently is the most common JSON Patch bug; pinned by `replace_missing_field_errors` distinct from `add_to_object_creates_field` tests. **Array `-` is `add`-only**: the `-` token means "after the last element" but only makes sense for `add` (append). Trying to `remove` or `replace` at `-` is meaningless; `parse_array_index` takes an `allow_dash: bool` flag and errors when used outside `add`. **Six ops covered**: add (insert + array-shift + dash-append + nested + root-replace), remove (object + array-shift-left + missing-error + root-cant-remove), replace (existing + missing-error + array-element), move (object + array), copy, test (pass-passes-through + fail-aborts). **Pure-fn `json_patch(doc, &[Value]) -> Result<Value>`** exposed publicly for callers using the operation outside the Tool trait — useful for state machines / config-update pipelines that want compile-time-typed access. Tools-utils now ships 34+ built-ins. 30 tests cover: pointer parsing (5 cases including the `~01` spec edge case), add (6 sub-cases), remove (4), replace (3), move (2), copy (1), test (2 — pass + abort), atomicity failure, tool integration (4 — basic + 3 missing-arg shapes), two realistic scenarios (config update with replace+add combo, workflow state with test precondition) |
| `SemanticDeduplicator` + `DedupResult` (iter 311) | Single batched `embeddings.embed_documents(&texts).await?` followed by inline `greedy_dedup_indices` (Rayon `par_iter().any()` short-circuit per candidate); cosine similarity inlined locally (the 6-line function isn't worth re-export reshuffle to make `transformers::cosine_sim` public); `DedupResult { kept, kept_indices, dropped_count }` triple so callers can correlate kept docs with original positions AND skip computing `original.len() - kept.len()` themselves; empty-input fast path skips the embedding round-trip; threshold clamped via `.clamp(0.0, 1.0)` since out-of-range values are programmer error not user error | **Closes the ingestion-time dedup gap that the existing in-tree primitives don't fully cover.** Three primitives now cover three distinct problems: `embedding_redundant_filter` (transformers.rs, iter ~30) is the in-memory math primitive — takes pre-computed embeddings, useful AFTER retrieval where the docs already carry vectors. `SemanticCachedRetriever` (iter 291) caches retrieval RESULTS by query similarity — different problem, "what to fetch" not "what to keep before storing". `SemanticDeduplicator` (this iter) is the end-to-end ingestion wrapper — one level up from the math primitive, embeds + filters in one call. **Why this matters at scale**: a scraped corpus with 20-30% near-duplicates wastes provider budget on duplicate embeddings (sometimes the most expensive line item in a RAG pipeline), inflates the vector store cost, and pollutes retrieval results — top-k results crowd with near-duplicate hits, which is what `embedding_redundant_filter` post-retrieval is meant to catch but is wasteful to do at retrieval time when you could've dropped them at ingestion. Industry-canonical pattern: dedup at ingest, not at query. **Order semantics pinned**: greedy iteration in input order means the EARLIER document of a duplicate pair survives. This matters for "keep canonical-source / drop scraper-mirror" workflows: feed the canonical sources first in the input list, scraper mirrors after, and the dedup naturally keeps the canonical. Pinned by `order_preserved_in_kept_list` and `near_duplicates_dropped_first_one_kept` tests. **Why greedy not optimal**: optimal dedup ("max subset where pairwise cosine < threshold") is the maximum-independent-set problem — NP-hard. Greedy is O(N²) cosine comparisons in the worst case but Rayon `par_iter().any()` short-circuits on first match, parallelism gives near-linear speedup on multi-core, and the results are indistinguishable from optimal on real corpora (the worst case where they diverge would require constructed adversarial input). The greedy approach also gives stable predictable output where callers can predict which doc survives — the optimal solution wouldn't have that property. **Threshold default 0.95** matches iter-291 SemanticCachedRetriever — pairs distinguishable from "verbatim near-duplicate after embedding noise" but lenient enough to catch real scraper-mirror cases (page reformatted with different navigation but same article body). Threshold tuning: lower for chattier-FAQ corpora where partial-overlap matters; higher for strict near-paraphrase only. **`kept_indices` field** is the diagnostic surface that pure `Vec<Document>` return wouldn't provide: callers can correlate the deduped output with their original input list (e.g., to update a metadata sidecar that tracks which scraper-mirror dropped, or to log "dedup removed 247 docs from /scrape_2024_q1/" with the original doc IDs). 9 tests cover: empty input → empty result (no embed call), 3 distinct docs all kept (orthogonal embeddings), near-duplicates dropped with first-kept (the order pin), threshold=1.0 only drops exact-cosine matches (0.999 stays), threshold=0.0 drops everything after first (any positive cosine triggers), threshold clamp ±, order preservation in kept list (`a-clone` after `a` → `a-clone` dropped not `a`), embedding-call failure propagates as fail-fast Error |
| `DebateAgent` + `DebateResult` + `DebateRound` (iter 310) | Round 0 + N refinement rounds via two parallel-fan-out helpers (`parallel_initial` and `parallel_round`) using `tokio::task::JoinSet<(usize, Result<String>)>` to preserve debater order on completion (each task returns `(idx, result)` so collected output can be shuffled into the right slots regardless of completion order); each round awaits all debaters before proceeding to the next (cross-round dependency means no parallelism there); judge runs serially after the last round; user-message format `"Question:\n{q}\n\nYour previous answer:\n{me}\n\nOther debaters' previous answers:\n--- Debater 2 ---\n{ans2}\n\n..."` so the model has clear visual separation between own-prior-answer, others' answers, and the question | **Closes the multi-model cross-checking gap.** Existing agent patterns (ReactAgent, PlanAndExecute, MapReduce/Refine, CritiqueRevise) all share a fundamental limitation: they're single-model. When a single model is *consistently* wrong about something — a specific factual gap, a reasoning hallucination it always makes the same way — self-critique can't surface the error because the same bias generates both the draft and the critique. Du et al. (2023) showed multi-model debate catches a different class of errors: different models (or same model with different sampling configs) have different bias patterns, and they cross-check each other's blind spots. Real prod scenarios: factual reasoning where one model has known training-data gaps; mathematical reasoning where models make consistent class-of-mistake errors; complex multi-step reasoning where verification through cross-checking outperforms single-model self-verification. **Architecture decision: parallel within rounds, sequential across rounds**. Within a round, all debaters can run concurrently (each only needs the *previous* round's outputs which are already finalized) — `JoinSet` fan-out gives near-linear speedup up to provider rate limits. Across rounds, sequencing is mandatory (round k+1 needs round k's outputs). Wall-clock pinned by `round_runs_in_parallel` test: 4 debaters at 50ms/call should complete a round in ~50ms not ~200ms. **Index-preserving collection**: the JoinSet returns futures in completion order, NOT spawn order. Without preservation, a 4-debater round could shuffle answers (debater 0's answer might appear in slot 2). The fix: each task returns `(idx, result)` and the collector uses `Vec<Option<String>>` indexed by original position. Pinned by `round_trace_records_per_round_outputs` test that asserts ordered output `[A-0, B-0]` for two debaters not `[B-0, A-0]`. **Same-model vs cross-provider debate**: passing the same `Arc<dyn ChatModel>` for all debaters gives sampling-temp diversity at minimal cost (Du et al's original paper used this); passing distinct models gives stronger diversity at higher operational complexity (separate API keys, separate rate limits, more failure modes). The choice is the user's. **rounds=0 fast path**: skips refinement; just initial answers + judge = `N+1` calls. Useful as a baseline for "is the debate actually adding value over a simple judge-of-N?" or for latency-sensitive paths. **Single-debater degenerate-but-works**: 1 debater with N rounds is essentially CritiqueRevise but with a separate judge model — useful when you want CritiqueRevise's structure but with a different judge than the drafter. **Failure propagation fail-fast**: any debater error short-circuits the round — partial answers from other debaters are discarded. Pinned by `debater_failure_propagates`. **Distinct from `RaceChatModel` (iter 184)**: race issues to N providers and returns first-success — single-shot, no round structure, no cross-checking. Debate is N rounds of cross-checking culminating in synthesis. The agents crate now ships 5 distinct chain/loop patterns: ReactAgent (tool-using loop, iter ~80), PlanAndExecuteAgent (plan-then-execute, iter 155), MapReduceSummarizer (multi-input parallel chain, iter 300), RefineSummarizer (multi-input sequential chain, iter 301), CritiqueReviseAgent (single-input self-improvement loop, iter 306), and now DebateAgent (multi-agent consensus, this iter). 9 tests cover: empty-debaters → InvalidInput, 2-debater × 1-round → 5 calls, 3-debater × 2-rounds → 10 calls, rounds=0 skips refinement (N+1 calls = just initial+judge), single-debater degenerate-but-works, within-round parallelism (4 debaters × 50ms ≤ 350ms wall-clock vs >450ms serial), index-preserving round trace, debater failure propagates fail-fast, default rounds==1 |
| `SlugifyTool` + `slugify` + `slugify_with` (iter 309) | Two-phase: (1) char walk that lowercases + maps each char through `strip_diacritic` (inline `match` table covering Latin-1 + extended-Latin a/c/d/e/g/h/i/j/k/l/n/o/r/s/t/u/w/y/z families plus ligature expansions æ→ae, œ→oe, ß→ss, þ→th), keeps ASCII alphanumerics, replaces runs of non-alphanumerics with the configured separator using a `last_was_sep` flag to suppress consecutive seps and leading seps; (2) optional truncation: if `buf.len() > max`, prefer to cut at the last separator within the prefix (`prefix.rfind(separator)`) for word-boundary truncation, fall back to byte-truncation if no separator exists in the prefix, then strip any trailing sep that landed at the cut | **Closes the URL-slug gap.** Real prod scenarios: blog/article CMS agents minting slugs from user titles; Git-branch-naming agents producing safe branch names from issue summaries; filesystem-safe filename generation from user queries; SEO URL generation from headings. Without a tool, the LLM does this per-character in its head — inconsistent across runs (sometimes drops emojis, sometimes preserves them; sometimes underscores, sometimes dashes; sometimes truncates mid-word, sometimes doesn't). Now it's a one-call canonical operation. **Why inline diacritic table not `unicode-normalization` crate**: the unicode-normalization crate weighs in at ~1 MB+ for the full Unicode database. The realistic agent-input space is Latin-script (English, Spanish, French, German, Polish, Czech, Slovak, Hungarian, Romanian, Croatian, Slovenian, Lithuanian, Latvian, Turkish, Vietnamese, Catalan, Italian, Portuguese, Maltese, Welsh, Icelandic, Esperanto, Frisian — all covered by the table). Non-Latin scripts (CJK, Cyrillic, Arabic, Hebrew, Devanagari, etc) get dropped to empty in the alphanumeric-filter phase, which is the correct behavior for ASCII-URL targets and a documented limitation in the tool docstring (callers needing Unicode-URL round-trips should percent-encode via `UrlParseTool` (iter 282) instead). **Ligature expansions** matter for European content: "Encyclopædia" → "encyclopaedia" (not "encyclop-dia"), "Œuvre" → "oeuvre", "Straße" → "strasse" (German ess-tsett). These are the realistic user-input cases where a naive ASCII-only filter would silently drop a character. **Word-boundary truncation** matters because mid-word cuts produce ugly slugs ("the-quick-brow" instead of "the-quick-brown"). The `prefix.rfind(separator)` lookup is O(n) but n is bounded by `max_length` so it's fine. Single-word inputs (no separator in the prefix) fall back to byte truncation since there's no word boundary to align to — pinned by test (`max_length_truncates_at_byte_when_no_separator_in_prefix`). **Pure-fn API exposed publicly** alongside the Tool trait so ingestion pipelines / doc-loaders can use the conversion without instantiating the tool. **Realistic-input fixtures** pinned (`realistic_blog_title`, `realistic_git_branch_name`) so a regression that breaks real-world cases is loud — the apostrophe in "Didn't" becomes a separator boundary correctly producing "didn-t" rather than "didnt" or breaking the loop. Tools-utils now ships 33+ built-ins. 21 tests cover: basic operation, punctuation collapse, leading/trailing strip, diacritic stripping (4 fixtures across European scripts), ligature expansions (æ/œ/ß), Nordic chars (ø/å), non-Latin dropping (Japanese/Cyrillic → empty), mixed Latin+non-Latin (non-Latin becomes separator boundary so ASCII parts survive), numbers preserved, underscores → separator (the trap where `_` is alphanumeric in some specs but not in slugs), custom separator (`_` and `.`), word-boundary truncation, byte-truncation fallback, max_length above input → no-op, empty / only-whitespace / only-punctuation → empty, realistic blog title fixture, realistic Git branch name fixture, tool-shape returns `{slug}`, custom-separator via tool args, tool max_length, missing text arg → InvalidInput |
| `pearson_correlation` / `spearman_correlation` / `correlate_scorers` + `ScorerCorrelation` (iter 308) | Pearson via standard formula `Σ(dx·dy) / sqrt(Σdx² · Σdy²)` with `Option<f64>` returns on length-mismatch / n<2 / zero-variance; Spearman builds on the existing `ranks(&[f64])` helper (fractional ranks with average-rank tie handling — same convention as iter-297 Wilcoxon's tie correction) then delegates to Pearson on the rank vectors; `correlate_scorers(&EvalReport)` collects per-scorer score vectors aligned by case index, filters to the case-intersection where BOTH scorers reported a value (`(Some, Some)` zip filter), runs the pairwise math; sorted output `(scorer_a, scorer_b)` for deterministic CI diffs | **Closes the "does my new metric agree with my trusted metric?" gate question.** Every team that ships a new auto-metric (chrF, custom regex scorer, faster LLM-judge, embedding-cosine variant) hits this gate: "I want to drop the slow scorer in CI and use this fast one — but only if they agree on rankings." Without correlation primitives, that's a manual diff in a notebook. With `correlate_scorers(&report)` it's a one-liner: `if all(spearman > 0.95) drop_slow_scorer`. **Why both Pearson and Spearman**: they answer different gating questions. Pearson captures *linear* agreement at constant ratio — required when downstream decisions care about magnitudes (a 0.85 vs 0.80 threshold gate cares about Pearson). Spearman captures *rank* agreement — required when only the ordering matters ("which auto-metric ranks the gold-set the same way as the trusted scorer? — pick that one"). The classic case where the two diverge: `y = x³` is strictly monotonic so Spearman=1 (perfect rank agreement), but Pearson < 1 because the relationship isn't linear. Pinned by the `spearman_perfect_monotonic_nonlinear` test which asserts `Pearson < Spearman` on this exact fixture. **Distinct from iter-290 McNemar / iter-297 Wilcoxon**: significance tests compare paired outcomes from THE SAME SCORER across TWO EVAL RUNS (baseline vs current) — "did the model change?". Correlation compares TWO DIFFERENT SCORERS on the SAME RUN — "do my scorers agree?". Different questions, both belong in the suite. **`Option<f64>` on undefined inputs**: callers can pattern-match on `None` to surface "not enough signal" without sentinel values like NaN that propagate silently through downstream math. Three triggers for `None`: length mismatch (programmer error), n<2 (correlation literally needs ≥ 2 points), zero variance (constant series — denominator collapses to 0; pinned by `pearson_constant_returns_none` and `spearman_constant_returns_none` tests). **Bool coercion** in `correlate_scorers` for pass/fail-style scorers — `true` → 1.0, `false` → 0.0 — so an `exact_match` boolean scorer can be correlated with a continuous `cosine_similarity` scorer without callers needing to pre-coerce. **Partial-coverage handling**: scorers that don't report on every case (sparse scoring is common — LLM-judge might only score a sample) get filtered to the intersection where BOTH scorers reported. Pinned by test (4 cases, scorer "b" only on 3 → n=3 in the correlation result, the missing case excluded). **Sort guarantee**: `(scorer_a, scorer_b)` lex-sorted output so CI diffs over correlation reports are stable across runs. The eval-decision toolkit now has 6 primitives: `detect_drift` (iter 289 — case-level surfacing) + `mcnemar_test` (iter 290 — binary significance) + `wilcoxon_signed_rank_test` (iter 297 — continuous significance) + `paired_effect_size` (iter 298 — magnitude) + `bootstrap_eval_ci` (iter 299 — uncertainty bounds) + `correlate_scorers` (this iter — scorer agreement). Multi-metric CI gates can stack arbitrarily across these. 18 tests cover: Pearson perfect positive / negative / uncorrelated, constant input → None, length mismatch → None, n<2 → None; Spearman perfect monotonic-nonlinear (the design pin), perfect monotonic decreasing → -1, tie handling (`xs=[1,2,2,3]` → ranks `[1,2.5,2.5,4]`), constant → None; `ranks` helper with and without ties; `correlate_scorers` perfect agreement, three-scorer fixture (3 pairs C(3,2)), partial-coverage (4 cases / scorer-b on 3 → n=3), constant-scorer → None, single-scorer → 0 pairs, bool coercion (true/false → 1/0 with valid correlation result) |
| `chrf` / `chrf_n` + `ChrfScore` (iter 307) | Same clipped-overlap pattern as ROUGE-N / BLEU but on `Vec<char>` after `to_lowercase().chars().filter(|c| !c.is_whitespace()).collect()`; per-n loop computes (P_n, R_n) via `HashMap<&[char], usize>` count maps + `Σ min(ref[g], cand[g])` clipped overlap; **skip-when-too-short rule**: when `ref_chars.len() < n` OR `cand_chars.len() < n`, that n contributes nothing to the macro average instead of forcing a zero (the latter would drag short-input scores toward zero artificially); macro-averaged chrP and chrR via `sum / count_of_active_n`; F-beta combination via `(1.0 + beta_sq) * P * R / (beta_sq * P + R)` with zero-denominator guard | **Closes the morphology / paraphrase gap left by ROUGE and BLEU.** The marquee win: ROUGE-1 of "running" vs "runs" returns 0 (different tokens); chrF returns > 0 because the substring "run" matches as a character n-gram. That's not a hack — it's the design intent. Word-level metrics treat inflectional variants ("walk" / "walked" / "walking" / "walks") as four distinct tokens with no shared signal. For morphologically rich languages (German compound words, Slavic case endings, Arabic roots) this is critical — chrF correlates substantially better with human judgment on translation eval there. For English, the gain is smaller but still real: paraphrases that swap word forms (singular ↔ plural, tense changes, synonyms with shared roots) get partial credit instead of being penalized as full mismatches. **Why this not METEOR**: METEOR (Banerjee & Lavie 2005) is an alternative paraphrase-aware metric using WordNet synonyms / stemming. METEOR has higher correlation with human judgment but requires WordNet (a 30 MB English-only lexical database) — language-specific, dep-heavy, and fundamentally word-level (still misses pure morphological variants where the stem is the same but inflection differs). chrF is language-agnostic, dep-free, and catches morphology directly. Both ship eventually but chrF is the better primary because it works for any language out of the box. **Why not skip-as-zero on short inputs** (the trap): a naive "if too short, contribute 0" implementation would have a 3-char identical input score < 1.0 because n=4..6 contribute zeros despite the input being literally identical. The skip-rule pin (`chrf_skip_n_when_too_short_doesnt_zero_score` test) verifies that short identical inputs score ≈ 1.0 — the macro average is over the n's that physically have grams, not all n's blindly. **β=2 default** is recall-weighted (matches Popović's empirically-best for translation eval where catching all reference content matters more than over-precision); β=1 gives standard balanced F1; β<1 weights precision more (rare in chrF use; documented as configurable). **Whitespace stripping** is a chrF preprocessing convention: char n-grams across word boundaries are equally informative for short n, and strict whitespace handling would penalize trivial spacing differences ("hello world" vs "helloworld"). Pinned by test. **Lowercase normalization** matches the rest of the evaluator family for consistency; documented as opinionated default vs Popović's strict case-sensitive reference impl. **Diagnostic struct** `ChrfScore { precision, recall, f_beta, beta }` echoes `beta` so cross-config result comparisons are self-describing — answers "you said chrF=0.85 but with which β?" without re-running. 11 tests cover: identical → 1.0, empty inputs (3 cases) → 0, disjoint-chars → 0, morphological-variant scoring (the headline test — ROUGE-1=0 but chrF>0 for "running"/"runs"), max_n=0 → 0, clipped-overlap-repeats (`ref="aa" cand="aaaaaa"` → p1=2/6 not 6/6, the same trap pinned in ROUGE/BLEU), β-weighting (β=2 vs β=0.5 on recall-heavy match), whitespace stripping ("hello world" vs "helloworld" → 1.0), lowercase normalization, struct beta echo, skip-when-too-short rule. The NLG-eval triad is now complete: ROUGE (word-level F, recall-emphasizing) + BLEU (word-level precision-weighted) + chrF (char-level F, recall-weighted, morphology-aware). Different tasks pick different metrics; multi-metric evals report all three to triangulate |
| `CritiqueReviseAgent` + `CritiqueReviseResult` + `CritiqueReviseIteration` (iter 306) | Single-input loop: `initial_draft = invoke(draft_prompt, input).await; for _ in 0..iterations { critique = invoke(critique_prompt, format!("User request:\n{input}\n\n--- Draft ---\n{current}")).await; current = invoke(revise_prompt, format!("User request:\n{input}\n\n--- Current draft ---\n{current}\n\n--- Critique ---\n{critique}")).await; trace.push(...) }` — sequential awaits, no spawning; errors short-circuit via `?` propagation; trace pushes BEFORE the revise call so it captures the draft-as-it-entered-the-critique-step (the natural debug-question shape: "what did the model think was wrong with THIS draft?") | **Closes the self-critique-loop gap left by the existing agent-pattern set.** ReactAgent (tool-using), PlanAndExecuteAgent (heterogeneous plan + execute phases), MapReduceSummarizer / RefineSummarizer (chunk-input chains) — all good but none capture the single-input self-improvement pattern that's common in production stacks: draft once, critique, revise, optionally iterate. Critique-revise has been shown to materially improve output quality on long-form writing, code review, and complex reasoning tasks where the cost of an extra round-trip is lower than the cost of shipping a flawed answer. The model itself often *can* see weaknesses in its own draft when explicitly prompted — it just doesn't naturally double-check in a single invocation. The critique step makes the self-check explicit. **Why three separate prompts (draft/critique/revise) not two**: a single "draft + revise" loop conflates "find issues" with "fix them" — the model has to do both in one pass, often punting on one or the other. Splitting them lets the critique step focus exclusively on issue-identification (without bias toward how easy it'd be to fix) and the revise step focus on integration (without re-deriving the issues from scratch). Each prompt is independently customizable so domain-specific evaluation criteria (legal-citation rigor, technical-doc API-name preservation, journalistic neutrality) can be wired into critique without contaminating draft style. **The iteration-trace shape pins a debug-friendly contract**: `iterations[i].draft_before` is the draft AS IT ENTERED iteration i (so iter[1].draft_before == revised output of iter[0], NOT the original draft). Pinned by test (`iteration_trace_records_pre_revise_drafts`) that verifies this — natural-question shape "show me the draft that triggered this critique" answered without recomputing. **iterations=0 fast path** returns initial draft directly with no critique work — useful as an A/B baseline (run with iterations=0 vs iterations=2 to measure how much the loop adds). **Anti-pattern guard in docstring**: not a win for short factual answers (one round-trip suffices, critique adds noise), tasks with crisp self-evaluable success criteria (math, code with tests — let the test gate it instead), or latency-sensitive paths (every iteration adds 2 round-trips). The agents crate now ships 4 chain/loop patterns: `MapReduceSummarizer` + `RefineSummarizer` (multi-input chains) + `PlanAndExecuteAgent` (plan-then-execute) + `CritiqueReviseAgent` (self-improvement loop). 7 tests cover: iterations=0 → draft-only (1 call, empty trace), iterations=1 → 3 calls with correct step indexing, iterations=N → 1+2N calls (verified at N=3 = 7 calls), iteration trace records pre-revise drafts (the contract pin), each step uses its own prompt (system-prefix substring match), error in critique propagates fail-fast (initial draft succeeds → critique fails → chain stops), default iterations==1 |
| `bleu` / `bleu_n` / `bleu_smoothed` + `BleuScore` (iter 305) | Reuses iter-304's `tokenize_lower` + `ngrams` + `count_map` helpers (composes cleanly — same n-gram + clipped-overlap machinery for both metrics, just different aggregation); per-n loop builds clipped overlap, divides by cand-grams length to get `p_n`; smoothing branch substitutes `1/(cand_grams.len() + 1)` for zero overlap (Chen & Cherry 2014 method-1 lite); geometric mean via `exp(Σ log(p_n) / max_n)` with `precisions.iter().any(== 0.0)` guard zeroing the result in strict mode; brevity penalty `(1.0 - r/c).exp()` when c < r else `1.0`; final `BLEU = BP · gm` | **Closes the BLEU gap iter-304's docstring explicitly flagged**: "BLEU is precision-weighted (translation eval) — could ship eventually but ROUGE is the primary need." Now both ship. The two metrics answer different questions: ROUGE asks "did the candidate cover the reference's content?" (recall-emphasizing — fits summarization where missing facts is worse than extras); BLEU asks "is the candidate's content actually in the reference?" (precision-weighted — fits translation where extras are hallucination). Different shapes of the same n-gram-overlap problem; multi-metric evals routinely report both because they expose different failure modes. **Why both strict + smoothed**: strict BLEU (any zero precision → 0) is the canonical behavior but degenerates on short sentences. A 4-token candidate against a 4-token reference can have p_4 = 0 if any single 4-gram differs, sending the score to 0 even when p_1 / p_2 are 0.75. That's noisy on short sentences but well-calibrated on long ones (paragraph-scale translations). Smoothed BLEU substitutes `1/(cand_grams + 1)` for zero overlaps — keeps short-sentence scores informative without inflating. NLTK's sentence_bleu defaults to a smoothed variant for this reason. The decision of which to use is task-dependent: corpus-level / long-form translations should use strict; sentence-level / short-form should use smoothed. **Brevity penalty is the second piece BLEU adds beyond clipped-precision**: without BP, a candidate could trivially achieve high precision by emitting only its most-confident token (a 1-token cand against a 100-token ref might have p_1 = 1.0). BP = `exp(1 - r/c)` when c < r exponentially penalizes short candidates — pinned by test (ref=7, cand=2 → BP = exp(-2.5) ≈ 0.0821). When c ≥ r, BP = 1 (no penalty for long candidates — over-generation is BLEU's blind spot, but unlike under-generation, an over-generation usually shows up as low precision through the n-gram overlap rather than needing a separate penalty term). **Diagnostic `BleuScore` struct** surfaces ref_len, cand_len, brevity_penalty, and the per-n precisions vector — answers "why is my BLEU 0?" with one of: cand is too short (BP < 1), cand has no overlapping bigrams (precisions[1] = 0), or both. **Case folding pinned**: BLEU is case-insensitive in this implementation matching ROUGE's convention (different from some BLEU implementations that case-sense). Documented as the choice and tested. 12 tests cover: identical strings → 1.0, known 5/6 fixture with full BLEU-4 calculation reproducing the reference `exp((ln(5/6) + ln(4/5) + ln(3/4) + ln(2/3))/4)`, empty inputs → 0, brevity-penalty isolation (max_n=1 fixture), no-BP for cand ≥ ref, strict zero on disjoint vocab, smoothed nonzero on sparse match (with sanity upper bound < 0.5 to catch implementation drift that could over-inflate), clipped-overlap-on-repeats (the same trap as ROUGE), max_n=0 → 0, max_n=1 isolation, lowercase normalization, struct field correctness |
| `rouge_1` / `rouge_2` / `rouge_n` / `rouge_l` + `RougeScore` (iter 304) | Tokenization via existing `tokenize_lower(s).collect::<Vec<_>>()` (lowercase + whitespace split — the convention used by every other evaluator in the file); n-gram extractor `(0..=tokens.len() - n).map(|i| tokens[i..i+n].to_vec()).collect()` — exits early on `tokens.len() < n` to avoid underflow; **clipped-count overlap** rule via `HashMap<&Vec<String>, usize>` count maps on each side, then `Σ min(ref[g], cand[g])` over each unique cand gram (the standard ROUGE / BLEU clipping rule that prevents "the the the" from over-rewarding repeated tokens); LCS DP via rolling-row arrays with the longer side on the outer axis for `O(min(m,n))` space; F1 derivation in `RougeScore::from_pr` with `0.0` when `P+R==0` to dodge division-by-zero | **Closes the NLG-eval gap.** The existing string-evaluators (exact_match, levenshtein, jaccard, regex, embedding_cosine) cover the binary / char-level / set-based / vector-based axes — but NOT n-gram overlap, which is the established summarization-eval metric in academic literature (Lin 2004 ROUGE — used by every paper from the original PEGASUS through to the latest Anthropic/OpenAI summarization eval reports). Without it, an eval harness comparing two summarizers had to either invent its own metric or fall back to LLM-judge (slow + non-deterministic). **Why ROUGE not BLEU**: BLEU is precision-weighted — designed for translation eval where producing extra correct content doesn't help. Summarization works the opposite way: a candidate that misses key facts is worse than one that adds extras (within reason); recall matters more, which is why ROUGE has been the de-facto summarization metric for two decades. BLEU could ship eventually but ROUGE is the primary need. **Why both N and L**: ROUGE-N captures local-order fidelity (bigram match means right pair in right order) but is brittle to paraphrase / reordering — "the cat sat" vs "sat the cat" share 0 bigrams. ROUGE-L allows gaps in the matched subsequence so paraphrases get partial credit. Different summarizers fail differently — some preserve facts but reorder; some keep order but skip facts; you want both metrics to triangulate. **The clipped-overlap trap is real and pinned by test**: a naive `Σ count_in_overlap` would let cand="the the the" with ref="the cat" score 3/3 precision (because cand has 3 "the"s and they "all match"). The clipping rule caps each gram's contribution at `min(ref_count, cand_count)`, so the candidate gets credited for ONE "the" — which is the genuinely correct answer (the candidate has 2 spurious extras). This is the standard rule from Papineni et al 2002 (BLEU) extended to ROUGE. The `rouge_n_clipped_overlap_repeats` test pins this with the canonical "the the the" fixture. **ROUGE-L gap-tolerance pinned**: `rouge_l_handles_gaps` test feeds ref="a b c d e" + cand="a x b y c z d w e" (matched tokens spread across insertions) → LCS=5, recall=1.0 (every reference token was matched, just with gaps), precision=5/9 (only 5 of 9 candidate tokens contributed). **ROUGE-L vs ROUGE-N divergence pinned**: `rouge_l_distinct_from_rouge_n_on_reordering` test feeds ref="a b c" + cand="c b a" (reversed) → ROUGE-2 F1=0 (no shared bigrams), ROUGE-L F1>0 (LCS finds the matching tokens). Proves L sees what N misses; both are needed in a multi-scorer eval. **Public API surface**: `rouge_1` and `rouge_2` are convenience shortcuts for the most-reported variants (every paper reports ROUGE-1 / ROUGE-2 / ROUGE-L); `rouge_n(.., n)` for callers wanting trigram or higher; `rouge_l` for the LCS variant. All return `RougeScore { precision, recall, f1 }` so callers can pick which they want — F1 is the headline number, P/R exposed for over/under-generation debugging. 13 tests cover: identical strings (P=R=F1=1), known 5/6 unigram fixture, known 4/5 bigram fixture, clipped-count-on-repeats trap, disjoint vocab → 0, empty inputs (3 cases), n=0 / n>token-count → 0, LCS gap-handling fixture, ROUGE-L vs ROUGE-N reordering divergence, case-folding normalization |
| `HtmlToTextTool` + `html_to_text` free fn (iter 303) | Pipeline of `regex::Regex` `Lazy`-statics: SCRIPT_BLOCK_RE / STYLE_BLOCK_RE / NOSCRIPT_BLOCK_RE (all `(?is)<tag\b[^>]*>.*?</tag>` non-greedy, case-insensitive, dot-matches-newline) drop those blocks WITH contents; COMMENT_RE (`(?s)<!--.*?-->`) drops comments; manual byte-walker scans for `<` and uses a `looks_like_tag_start` guard (`<` followed by alpha / `/` / `!`) before extracting up to the next `>` — guard prevents the unterminated-`<` (e.g. "Price < 5 dollars") from gobbling everything up to a far-away `>`; classified tags push `\n\n` (block-level) or `\n` (br) and OTHER tags pass through to the generic `<[^>]+>` strip pass; entity decoder (`&amp;` `&lt;` `&gt;` `&quot;` `&apos;` `&nbsp;` + `&#NN;` decimal + `&#xHH;` hex via `u32::from_str_radix(_, 16)`) with unknown entities deliberately left literal; horizontal-space collapse `[ \t]+` → ` ` then per-line `trim_end` then `\n{3,}` → `\n\n` then top-level trim | **Closes the "fetched HTML → clean prose" gap.** WebFetchTool (iter 151) returns raw HTML; agents that want to summarize, embed, or quote from a page have nowhere to go except a custom extractor or the LLM mentally parsing tags (which it does poorly — often confuses content with `<span>` chrome). HtmlToTextTool is the bridge. Real prod scenarios: agent fetches a docs page → strips → summarizes via MapReduceSummarizer (iter 300); RAG ingestion pipeline embedding clean prose without `<div>` boilerplate inflating dimensions; agents quoting web sources cleanly. **Why regex not html5ever**: html5ever is the real-deal spec parser with proper error recovery, but pulls ~30 transitive deps including bytes/lazy_static/markup5ever/string_cache/etc. For strip-and-clean scope (no DOM queries, no element selection by CSS), regex over a small sequence of well-defined patterns produces equivalent output on real-world HTML at near-zero dep cost. iter-293 HtmlSectionSplitter already established this approach in the splitters crate; this iter applies it to the tool-axis. **Pinned correctness traps** (caught in initial test run, fixed before commit): (1) `<br/>` self-closing tags — naive `name_end = inner.find(whitespace).unwrap_or(inner.len())` returns inner.len() for `br/`, so the lookup matches "br/" not "br" and misses the soft-break case; fixed by adding `/` to the name-end terminators. (2) Unterminated `<` ("Price < 5 dollars" embedded in a real doc) — the tag walker finding the next `>` somewhere far down meant a literal less-than gobbled all intervening prose; fixed by adding `looks_like_tag_start` that requires `<` to be followed by alpha / `/` / `!` to count as a tag boundary. (3) Unknown entities (`&hellip;`) — decoder must leave them literal rather than over-eagerly substituting; better to ship a `&hellip;` than corrupt the document. **Block-level set** is curated to LLM-relevant boundaries: `p / div / section / article / main / aside / header / footer / nav / h1-h6 / li / tr / blockquote / pre`. Inline tags (`strong`, `em`, `span`, `a`) deliberately pass through to the generic strip — preserving the inline text continuity. **Pure-fn `html_to_text(&str) -> String` exposed publicly** so callers can drop the conversion outside the Tool trait — useful for ingestion pipelines, doc-loaders that fetch HTML, etc. Tools-utils now ships 32+ built-ins. 19 tests cover: script+style stripping (verifies CONTENTS removed not just tags), comment stripping, paragraph boundaries, br soft newline (with the self-closing trap), list items, headings, entity decoding (6 named + numeric dec + numeric hex), unknown-entity literal preservation, blank-line collapse capped at 2, horizontal-space collapse, inline-tag preservation, attribute handling, nested blocks, empty/whitespace input, plain-text passthrough, unterminated-`<` literal, tool-shape returns `{text, char_count}`, missing-arg error |
| `DatetimeParseTool` (iter 302) | Trim+lowercase normalization of input → match-arm dispatch over `today/yesterday/tomorrow/now`, `RFC 3339` parse, `NaiveDateTime::parse_from_str("%Y-%m-%dT%H:%M:%S")`, `NaiveDate::parse_from_str("%Y-%m-%d")` (date-only path applies the FixedOffset's local-time mapping); regex-free splitter for `"N <unit> ago"` / `"in N <unit>"` (`splitn(2, ' ')` → integer parse + unit-word lookup with plural-`s` strip); weekday phrase parser handles `last/next/<bare>` with explicit same-day-jump semantics (`last wed` from a Wed jumps back 7 days, not returns today — pinned by test) | **Closes the natural-language date-parse gap that iter-279 CurrentTimeTool created**: `CurrentTimeTool` gives the agent the current moment, but agents constantly receive *user-provided* date strings ("show errors from last Tuesday", "fetch metrics for the past 3 days", "what was the close price March 15?") that they currently have to do manual date arithmetic against the current time to resolve. That arithmetic is brittle and fails for anything beyond "today / yesterday." `DatetimeParseTool` is the answer: feed it any natural-language date and get a normalized `{iso8601, date, weekday, unix}` object the agent can use directly in SQL queries / log filters / API params. Real prod scenarios: support agents reading user reports ("the bug started last Friday"); analytics agents pulling time-bounded metrics ("last 2 weeks of conversion data"); calendar/scheduling agents ("schedule for next Tuesday at 2pm"); financial agents looking up historical prices. The `reference` arg lets the tool be deterministic against a fixed point — useful for batch-processing historical data where "yesterday" should mean "yesterday relative to record_date," not "yesterday relative to now." Intentional non-coverage of quarter literals (`Q1 2025` is too ambiguous — Q1 of fiscal year? calendar?), IANA tz names (would pull in chrono-tz's IANA database — explicit-offset is enough for most prod cases), holiday names (would need a calendar — varies by country), and fuzzy/typo tolerance (the agent should retry with corrected wording — surface failures loudly so the LLM can see and adapt). The `same-weekday-jump` semantics matter: a naive impl where `last wednesday` from a Wednesday reference returns today would silently miscount events that happened a week ago — pinned by the `last_wednesday_jumps_back_a_week` test. The `bare-weekday` semantics ("Tuesday" alone = most-recent past Tuesday) match how English speakers refer to recent past events when they don't specify "next" — also pinned by test. Months/years are approximated as 30/365 days because chrono::Duration is fixed-time-units (no calendar-aware month math without chrono::Months and the more complex DateTime arithmetic) — explicit in the docstring so callers know to use exact dates when calendar precision matters. 19 tests cover today/yesterday/tomorrow/now (with start-of-day for date-only outputs vs full time for "now"), N-units-ago in 3 directions (days/weeks/hours), in-N-days forward, last/next weekday in both directions, the two same-weekday edge cases, bare-weekday → most-recent-past, ISO 8601 passthrough, YYYY-MM-DD passthrough, IST tz-offset (5.5 fractional hours) applied to a YYYY-MM-DD input → output zone is +05:30 not +00:00, defaults-to-now when no reference, unsupported input → InvalidInput with the supported-patterns list, missing input → InvalidInput. Tools-utils crate now ships 31+ built-ins |
| `RefineSummarizer` (iter 301) | Sequential awaits — `initial = invoke(initial_prompt, chunks[0]).await; for c in &chunks[1..] { running = invoke(refine_prompt, format!("Existing summary:\n{running}\n\n--- New section ---\n{c}")).await; }` — no spawning, no semaphore (sequential by design); errors short-circuit via `?` propagation | **Closes the chain-pattern pair started in iter 300.** Iter-300's docstring explicitly flagged this as future work: "this iter ships map-reduce, refine remains future work." Now both ship together. The two patterns answer different questions: map-reduce parallelizes but loses cross-chunk context (each map call sees only its own chunk); refine preserves cross-chunk context but parallelizes nothing (each refine call sees the running summary, so an event mentioned in chunk 3 and referenced in chunk 7 survives into the final summary). Pick refine when fidelity-of-narrative matters and you can afford the latency; pick map-reduce when chunk count is high and bullet-point fact extraction is the goal. The user-message format is intentionally explicit: `"Existing summary:\n{running}\n\n--- New section ---\n{chunk}"` so the model has a clear visual separator between what's already-summarized and what's new — a single concatenated blob would let the model conflate "what I already know" with "what I just read." Default prompts cover general-document use; override via `with_initial_prompt` / `with_refine_prompt` for domain tone (legal-style citation preservation, journalistic neutral voice, technical-doc API-name preservation). **Why `Result<String>` and not a stream of intermediate states**: streaming the running summary as it evolves is tempting (the user could watch it form) but misleading — chunks 1..k can produce a summary that's contradicted by chunk k+1, which a user-watching-streaming would see and confuse for the final answer. Callers who genuinely want progress observability should compose with iter-199 `Progress` externally. **Error propagation is fail-fast**: any chunk's call failure stops the chain (the running summary up to that point is discarded — by design, since a partial-document summary is misleading). Verified by `FailOnSecondModel` that errors on call 2 → chain stops at 2 calls. 7 tests cover: empty/single/N-chunk paths, running-summary carry-forward (4th call sees `S2`, not the original chunks — pins the contract that refine actually refines), error mid-chain (chunk 3 unprocessed), custom prompts wired through (system-prefix substring match), and a wall-clock test that 5 chunks @ 30ms each take ≥ 140ms — verifies refine is sequential, not accidentally parallel. The agents crate now ships two summarization chains; refine + map-reduce together cover the long-doc summarization landscape |
| `MapReduceSummarizer` (iter 300) — milestone | `tokio::sync::Semaphore` for bounded map-phase fan-out (default cap = 8); each map task gets `acquire_owned()` permit, runs `chat_model.invoke([Message::system(map_prompt), Message::user(chunk)])`, releases on drop; collected via `tokio::spawn` + `JoinHandle.await` loop (failure on any task short-circuits the others — a panicked task surfaces as `Error::Other`); reduce phase composes summaries with `\n\n---\n\n` separator and runs the same single-call pattern; recursive-reduce mode groups via `summaries.chunks(group_size)` where `group_size = summaries.len().div_ceil(2)`, tail-recurses through `Box::pin(self.reduce_phase(group_summaries)).await` (Box::pin needed because async-fn recursion isn't allowed without indirection) | **Iter-300 milestone — first chain primitive in the agents crate.** Closes the "summarize long docs" gap that LangChain solves with `MapReduceDocumentsChain`. Real prod use: distilling book-scale corpora (legal docs, contracts, papers) into briefs; multi-hour transcript → executive summary; large-codebase changelog generation; agent reading a long fetched doc to extract key points before answering a follow-up. **Why map-reduce over stuff-documents**: stuff just concatenates everything into one prompt — works while total < context window, fails for inputs over that. Map-reduce handles inputs of arbitrary length by trading latency (multiple model calls) and a small fidelity loss (per-chunk summaries discard cross-chunk context) for unbounded scale. **Why map-reduce over refine**: refine passes a running summary through chunks sequentially, preserving cross-chunk context but parallelizing nothing — every step waits for the prior. Map-reduce parallelizes the expensive map phase, which matters when chunk count is high and the model is the latency bottleneck. The two patterns answer different questions; this iter ships map-reduce, refine remains future work. **Termination guarantee**: only recurses when `summaries.len() > 2`, otherwise grouping wouldn't actually shrink the input — caught in development as a stack-overflow when group_size=1 produced two single-summary "groups" that tail-recursed forever on inputs that still exceeded the threshold. The fix is a 1-line guard with a tested pinning case (the `recursive_reduce_for_long_combined_output` test exercises 6 chunks at a 10-char threshold which forces real recursion: 6 map + 2 group-reduce + 1 final-reduce = 9 calls). **Default-prompts policy**: ships sensible map and reduce prompts so callers can use the primitive without writing prompts (matches `litgraph-loaders` defaults policy elsewhere); override via builders for domain-specific tone (legal-style, journalistic, bullet-points, etc). **Bounded fan-out via Semaphore (default 8)**: provider TPM caps mean unbounded fan-out causes 429 storms; the cap throttles per-call. For global cross-summarizer budgets, compose with iter-246 `SharedRateLimitedChatModel` in the inner Arc<dyn ChatModel>. **Single-chunk fast path** skips map phase and runs reduce-prompt directly — saves one round-trip on inputs that already fit. **Empty fast path** returns "" without any LLM calls. 8 tests cover: empty/single/three-chunk paths, recursive-reduce call counts (9 calls for 6 chunks at low threshold), recursive-off (7 calls = 6 map + 1 reduce), max_concurrent=0 clamps to 1, custom prompts wired through (verified by a model that branches on a substring), and a wall-clock timing test (8 chunks at 4-wide concurrency over 50ms-per-call models completes < 350ms vs > 400ms serial — proves the semaphore actually parallelizes) |
| `bootstrap_eval_ci` + `bootstrap_mean_ci` + `ConfidenceInterval` + inline `Xorshift64` PRNG (iter 299) | Per-scorer collect-then-resample: gather scores into a `Vec<f64>`, run `n_resamples` draws-with-replacement (each draw `n` indices via `rng.next_in(n) = (xorshift64.next_u64() as usize) % n`), compute resample mean, sort the bootstrap means, take the `((1-confidence)/2 * (n_resamples-1)).round()` and `((1-(1-confidence)/2) * (n_resamples-1)).round()` indices for lower/upper bounds (the percentile method); xorshift64 lifted from Marsaglia 2003 (4-line core: `x ^= x<<13; x ^= x>>7; x ^= x<<17`) seeded from a `u64` arg with the golden-ratio mix constant `0x9E3779B97F4A7C15` substituted for seed=0 (xorshift's degenerate state); confidence clamped to `(ε, 1-ε)`, n_resamples clamped to `>= 1` | **Closes the "0.83 ± what?" gap.** Point-estimate aggregates (`means.cosine = 0.83`) are silent about uncertainty — whether the score is rock-solid or one-case-from-flipping is invisible. Bootstrap CI surfaces it without distributional assumptions. Pairs with the existing eval-decision toolkit: detect_drift (iter 289, case-level surfacing), McNemar (iter 290, binary significance), Wilcoxon (iter 297, continuous significance), paired_effect_size (iter 298, magnitude). Now the canonical eval CI report can ship `mean ± CI` per scorer — diffable across runs because bootstrap is seed-deterministic. **Why bootstrap not t-distribution CI**: t-CI assumes the underlying score distribution is roughly Normal. Eval scores often aren't — LLM-judge scores cluster near 0 or 1, exact-match is binary, cosine bounded [-1,1] with skewed tails. Bootstrap is non-parametric — works for any distribution including the skewed and bounded ones. **Why bootstrap not analytical SE-of-the-mean**: SE = std/√n works for the Normal mean; for functionals (median, percentile, AUC, Cohen's d) the analytical SE gets messy. Bootstrap gives a CI for ANY statistic via the same recipe — though this iter ships only the mean to keep the API tight. Future iters can extend to median/percentile bootstraps using the same xorshift PRNG. **Why xorshift not the `rand` crate**: bootstrap doesn't need crypto-grade randomness — only enough quality to make resamples uncorrelated. xorshift64 has a 2^64-1 period, fits in 4 lines of code, passes standard chi-squared / runs tests, and avoids pulling `rand` into `litgraph-core`'s deps. The dep-light philosophy of the eval-significance / eval-effect-size modules continues. **Reproducibility**: same `(values, n_resamples, confidence, seed)` → bit-identical bounds. CI eval reports diffable across runs. `bootstrap_mean_ci(&[f64], n, c, seed) -> (mean, lower, upper)` is exposed publicly for callers wanting a CI on a metric outside the eval harness — latency runs, per-request token counts, any `Vec<f64>`. 11 tests cover: empty input → zeros; all-identical / single-value → CI collapses to point (the CI doesn't lie about uncertainty, it just has nothing to draw from); CI brackets the true mean for uniform 0..1 data with sub-0.2 width; same-seed reproducibility; different-seeds-different-bounds; higher-confidence-widens-CI (99% > 90%); n_resamples=0 clamps to 1; confidence clamped to (ε, 1-ε); per-scorer alpha-sort with two-scorer fixture; xorshift-seed-zero substitution (verifies the constructor catches the degenerate state) |
| `paired_effect_size` + `PairedEffectSizeResult` (iter 298) | Per-scorer paired-diff calc (same pairing scheme as iter-289 drift, iter-290 McNemar, iter-297 Wilcoxon — pair by `input` string, skip missing-in-current); sample standard deviation with n-1 denominator (`(xs - mean)²/(n-1)` then sqrt); Cohen's d_z = mean/std; Hedges' g multiplied by `(1 - 3/(4n-5))` small-sample correction; magnitude band lookup over absolute d | **Closes the "is it big enough to care?" gap that complements significance.** With a large eval set, a 0.001 cosine shift over 10000 cases is statistically significant via Wilcoxon — and practically meaningless. Effect size is the antidote: a normalized magnitude that does NOT grow with sample size. Now any eval CI gate can stack the two filters cleanly: `wilcoxon.significant_at_05 && effect.magnitude in {"small","medium","large"}` — filter noise first, then filter trivia. Both tests live in `litgraph_core` and re-export from the crate root, alongside `mcnemar_test` (binary significance), `detect_drift` (case-level surfacing), and the eval harness itself. Why d_z (paired-Cohen) over d_av (average-of-stds): d_z matches the "same case under two treatments" eval workflow — it's the effect size the paired t-test's noncentrality parameter is built on. d_av is for between-groups comparisons. Why include Hedges' g alongside d: d is biased upward for small n; Hedges' multiplies by `(1 - 3/(4n-5))` which is 0.8 at n=5, 0.91 at n=10, 0.97 at n=25, 0.99 at n=50 — for n>50 callers can ignore g and read d directly, but for typical eval sets of 10-50 cases the correction matters. Key edge case: **uniform shift with zero std** (every diff is the same nonzero value) surfaces as NaN d + magnitude `"undefined"`, NOT as a finite "huge" effect. That's the right semantics — Cohen's d is mean-relative-to-spread, and zero spread means "there's nothing to compare the mean to." Significance tests interpret zero-spread differently (Wilcoxon would call it highly significant since W=0). The two metrics genuinely answer different questions; users should consult both. 10 tests cover: no-change → undefined; uniform shift → undefined (the textbook trap); hand-calculated d ≈ 1.265 from a 5-case fixture with diffs [0.0, 0.1, 0.2, 0.3, 0.4] → "large" label; sign mirror for regression (negative d, same magnitude); Hedges correction at n=5 (g/d == 0.8 exactly); Hedges correction at n=100 (~0.9924); magnitude-band boundaries (verifies < 0.2 → negligible, 0.2 → small, 0.5 → medium, 0.8 → large); missing-in-current case-pairing convention; n=1 undefined; per-scorer alphabetical sort with mixed alpha-improved + zeta-regressed |
| `wilcoxon_signed_rank_test` + `WilcoxonResult` (iter 297) | Per-scorer paired-diff calc → drop zeros → rank `|diff|` with `partial_cmp` sort + average-rank ties pass → split sums into W+/W- → `W = min(W+, W-)` → normal approximation with **tie-corrected σ²** (`Σ(t³−t)/48` subtracted) and continuity correction (+0.5 toward μ); two-tailed p via shared `normal_cdf` (Abramowitz & Stegun 7.1.26 erf approximation, same as iter-290 McNemar). No new dep — keeps the "no statrs" stance of the eval-significance module | **Closes the eval-significance pair: McNemar (iter 290) for binary, Wilcoxon (this iter) for continuous.** McNemar's own docstring already pointed at this gap: "for continuous scorers, use a paired t-test or Wilcoxon" — but Wilcoxon wasn't actually built-in. Now any continuous scorer (cosine similarity, BLEU, raw LLM-judge floats, embedding-recall@k, perplexity diffs) has a properly-calibrated significance test that produces the same `significant_at_05: bool` CI-gate flag as McNemar. Why Wilcoxon over a paired t-test: t-test assumes diffs are normally distributed, but eval-score diffs are usually skewed (most cases unchanged, a handful regress sharply); Wilcoxon makes no normality assumption (only symmetric-around-median under H₀), so it stays well-calibrated under skew where t-test would inflate false positives. Output design mirrors McNemar's: `n` (non-zero pair count), `w_plus`/`w_minus` for inspection, `w` (test statistic), `mean_diff` (directional summary across ALL pairs including zeros, since callers usually want this regardless of significance), `z`, `p_value`, `significant_at_05`, `small_sample` (n < 20 → asymptotic approximation unreliable, recommend exact-table or permutation test). Tie handling implements the textbook average-rank rule: when k values tie at an absolute rank, all k get the average of the ranks they would otherwise span. The variance correction `Σ(t³−t)/48` adjusts σ² downward — without it, the test would be overconfident under heavy ties. 9 tests cover: no-diffs (p=1, n=0, mean_diff=0), uniform improvement of 25 cases (W=0, highly significant, mean_diff=+0.1), uniform regression mirror, balanced-flip 10/10 (W=105 = mean_w → not significant), small-sample flag (n=10), textbook example with W+=17.5, W-=10.5 (verifies ranking math + tie handling), missing-in-current skipped silently, zero-diffs dropped (n excludes them but mean_diff includes), per-scorer alphabetical sort. Both McNemar and Wilcoxon now live in `litgraph_core::eval_significance` and re-export from the crate root |
| `CachedChatModel` (iter 296) | `parking_lot::Mutex<Vec<(String, CachedChatEntry)>>` linear-scan LRU; cache key via `exchange_hash` (blake3 over canonical JSON of `(messages, ChatOptions)`) — the same function the cassette infrastructure uses, so the cache and cassettes agree on what constitutes "the same request"; on-lock TTL sweep + per-call evict-while-over-cap | **Closes the chat-cache gap.** Existing primitives covered the test path (`RecordingChatModel`/`ReplayingChatModel`, iter 254 — record once, replay forever in CI) and the embed axis (`CachedEmbeddings`, iter 292). The chat axis had no live production cache — agents and eval harnesses re-issuing identical requests paid for them every time. Distinct from `PromptCachingChatModel` (iter 136) which controls Anthropic's *server-side* prompt cache via cache_control headers — that reduces input-token cost on the provider side; this caches the entire response client-side, so identical requests skip the network roundtrip entirely. The two compose: client-side cache hits skip the call entirely; misses with prompt caching still get input-token savings upstream. Streaming intentionally bypasses (token streams can't be replayed without preserving inter-chunk timing, and stream callers want streaming UX — they'd use `invoke()` if they wanted a single response). Win conditions: eval harness re-runs over the same dataset (parameter sweeps, scorer iteration), FAQ agents where users phrase the same question identically, demo/dev environments while iterating on downstream UI, multi-stage agent loops where retry/restart re-issues the same upstream call. Anti-pattern: high-temperature creative generation (cache-hit rate ~0%), long-tail prompts where each query is unique. 8 tests cover: identical-call hit, different-message miss, different-opts miss (verifies the canonical-hash includes ChatOptions), LRU eviction (cap=2 with 3 inserts, oldest evicted, re-invoking it re-calls inner), TTL expiry (50ms TTL, sleep 80ms, second call re-invokes), `clear()` drops all entries, `name()` delegates, `max_entries=0` clamps to 1 |
| `JwtDecodeTool` (iter 295) | `URL_SAFE_NO_PAD` base64 + `serde_json::from_slice` per segment; `exp`-claim comparison against `SystemTime::now()` for expiry detection; signature segment optionality (2-segment `header.payload` accepted, 3-segment with empty `signature` also valid) | **Closes the JWT-inspection one-shot vs the prior 3-call chain (string.split + Base64Tool + json_extract).** Real prod scenarios: agent extracting `sub`/`user_id` from a request bearer token to scope its tool calls; debugging "why did this OAuth callback 401?" by inspecting `iss`/`aud`/`exp`/`scope` claims; checking expiry before a retry storm. Returns a structured `{header, payload, signature_present, expired}` object — `expired` is `null` when `exp` is missing (RFC 7519 §4.1.4 — `exp` is OPTIONAL), `bool` otherwise. **Signature verification is intentionally out-of-scope.** Verifying a JWT requires the issuer's signing key, which an agent doesn't generally have access to and shouldn't be juggling at the tool level. The docstring loudly warns that this tool is for *inspection*, not *authentication* — verification belongs in the auth layer (server-side middleware before the agent ever sees the token). If your agent ever uses the result of this tool to authorize anything, that's a bug. The `URL_SAFE_NO_PAD` choice matches the JWT spec — RFC 7515 §2 explicitly mandates "Base64url" with no padding. The standard padded variant would reject most real-world JWTs, so picking `URL_SAFE_NO_PAD` keeps the tool useful out-of-the-box. Verified by 10 tests including the canonical RFC 7519 §A.1 standard test vector (`eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJqb2UiLC...`) which decodes to header `{"alg":"HS256","typ":"JWT"}` and payload `{"iss":"joe","exp":1300819380,...}` — both bit-exact, expired-flag correctly true. Tools-utils now ships 30+ built-ins |

---

## Out-of-band research log

Findings worth keeping around as we plan future iters.

- **LangChain `EnsembleRetriever`** — equal weights default, but
  the usable form takes `weights: list[float]`. Internally
  sequential; each `get_relevant_documents` call serial under GIL.
  We can match the API and beat the perf trivially.
- **LangGraph `Functional API`** (Sept 2024) — `@entrypoint` /
  `@task`. Implemented purely as a graph-building convenience layer
  over `StateGraph`. Their docs explicitly call out: "every
  functional API workflow desugars to a StateGraph." That makes
  this a Python-only port for us.
- **mistralrs vs candle for #8** — `mistralrs-core` ships a
  high-level `Engine` with KV-cache + paged attention + speculative
  decoding. `candle` is closer to the metal but every project ends
  up rebuilding the same engine. Pick `mistralrs` unless we hit a
  licensing wall.
- **pyo3-stub-gen** — works on PyO3 0.22 (we're on 0.28). Need to
  verify compatibility before adopting; otherwise the manual
  `litgraph-stubs/*.pyi` flow stays.
- **Webhook-resume design** — should re-use `Command::resume(value)`
  and `Command::goto(node)` so we don't invent a third execution
  control surface.
